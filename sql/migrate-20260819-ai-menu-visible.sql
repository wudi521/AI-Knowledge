-- ============================================================
-- 显示 AI 大模型菜单目录(2026-08-19)
-- 目标库: ruoyi-vue-pro (MySQL 8.0)
-- 背景: 2758「AI 大模型」目录 visible=0 导致 AI 知识库/评测管理/检索测试等
--       子菜单在侧边栏不可见(用户找不到评测入口)
-- 幂等: 重复执行无副作用
-- ============================================================
UPDATE `system_menu` SET `visible` = b'1' WHERE `id` = 2758;
