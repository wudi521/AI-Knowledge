# 多轮对答与上下文实施计划(6 任务)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** 生产级多轮对答: 历史结构化传递(chat→evidence→retrieval 消歧 + 生成带历史)、上下文截断与 token 控制、history 快照落库、会话摘要, 端到端可验证。

**Spec:** `docs/superpowers/specs/2026-08-18-多轮上下文-design.md`

---

## 环境前提
- chat-server(48083)/evidence-server(48087)/retrieval-server(48086)/model-server(48091) 已交付单轮链路
- ai_message 已落库(历史来源);ai_conversation 有 summary/transfer_reason/operator_id
- yaml 已有 `yudao.chat.max-context-messages: 6`(未生效, 需绑定)

## 任务

### Task 1: DTO 链路 + 透传 + history 快照落库
- evidence-api: `ChatTurnDTO{role, content}`;EvidenceEvaluateReqDTO 加 `List<ChatTurnDTO> history`;EvidenceEvaluateRespDTO 加 `List<ChatTurnDTO> history`(回显)
- retrieval-api: 自身 `ChatTurnDTO`(同构);RetrievalSearchReqDTO 加 `List<ChatTurnDTO> history`
- evidence-server: EvidenceService.evaluate 加 history 参数(重载, 旧签名委托);EvidenceAssembler.assemble 加 history → RetrievalSearchReqDTO.setHistory;EvidenceApiImpl 映射 history
- evidence-server: EvidenceService 落库 ai_evidence_eval 时 history 快照(JSONUtil.toJsonStr);EvidenceEvalDO 加 history 字段 + DDL `ALTER TABLE ai_evidence_eval ADD COLUMN history json NULL COMMENT '评估上下文快照'`
- retrieval-server: SearchService.search 加 history 重载(旧签名保留);透传给 QueryAnalysis
- 编译 + 启动冒烟(单轮兼容: history=null 行为不变)

### Task 2: QueryAnalysis 消歧
- QueryAnalysis.analyze(query, history): user prompt 结构化为"历史对话区 + 当前问题";system 要求 rewrites 结合历史展开指代、products/entities 继承
- 结构化兜底: LLM 失败且历史非空 → 当前问题 + 最近 USER 消息拼接为 rewrite
- 验证: "那换屏多少钱" + 历史"X100 Pro 碎屏能免费修吗" → rewrites 含"X100 Pro 换屏 多少钱"/products=[X100 Pro]

### Task 3: 生成带历史
- AnswerGenerator.generate(query, evidences, history): prompt 附历史(截断后, 每条≤200字总≤2000字, "仅供指代理解, 不要复述")
- ClaimVerifier.verify(query, answer, evidences, history): 同上
- AnswerPipeline.generateWithClaims(query, evidences, history) 透传
- 编译验证

### Task 4: ChatPipeline 上下文 + 配置绑定 + 计数
- MessageService.getRecentMessages(conversationId, limit): 取最近 N 条 USER/AI(排除 SYSTEM), create_time asc
- ChatPipeline: 读历史 → 截断(轮数/条长/总长)→ 构造 List<ChatTurnDTO> → evaluate(..., history)
- `yudao.chat.max-context-messages` 绑定(@ConfigurationProperties ChatProperties 或并入 TransferProperties 改名 ChatProperties;核对 yaml 键)
- ConversationService: messageCount 计数(DDL `ADD COLUMN message_count int NOT NULL DEFAULT 0`;addMessage 后自增)——简化: 查询 COUNT 也可, 决策选 count 字段增量更新
- 编译验证

### Task 5: TransferHandler 摘要增强 + context_summary
- buildSummary 附"近轮对话要点": 从历史取最近 3 条 USER+AI 规则拼接(不调 LLM)
- ai_conversation 加 `context_summary varchar(1000)`;manualTransfer/handleTransfer 时写入
- 编译验证

### Task 6: 端到端验证 MC-01~MC-06
- MC-01 多轮追问: 轮1"X100 Pro 碎屏能免费修吗" → 轮2"那换屏多少钱?" → 答 ¥699
- MC-02 history 快照落库可查(evidence_eval.history 含轮1)
- MC-03 SYSTEM 消息不进上下文
- MC-04 超长会话截断生效不报错
- MC-05 单轮兼容(无 history 行为不变)
- MC-06 context_summary 落库
- 前端对话工作台多轮体验(消息流已有, 无需改;确认)

---

## 自审记录
- 历史结构化传递(不拼脏串);消歧前置(检索前), 检索纯净
- 生成带历史但判定不带(充分性/冲突仅对当轮)
- 三级截断 + 角色过滤(SYSTEM 排除)
- history 快照落库(评测/重放);context_summary(坐席)
- 向后兼容: 新参数全可选

---

## 验证记录(多轮上下文 MC-01~MC-06)

> Task 6 端到端验证, 2026-08-18 晚。HEAD=8e19ede(T5)。验证方式: 当前代码打包(boot jar)起 48088(evidence)/48089(retrieval)/48093(chat) 空闲端口实例, 直接 RPC/HTTP 验证; 用户常驻进程未动。

### 构建新旧判定(关键前提)
| 服务 | 端口 | 启动时间 | 判定 | 依据 |
|---|---|---|---|---|
| system | 48081 | 11:47 | 无需重起(T1~T5 未改 system) | — |
| retrieval | 48086 | 18:53:38 | **旧构建** | 早于 T1~T5 提交(19:12~19:30) |
| chat | 48083 | 18:54:10 | **旧构建** | 同上; 其 evaluate 调用落库 history=NULL(旧 evidence 丢弃 history 字段) |
| evidence | 48087 | 18:59:01 | **旧构建** | evaluate-rpc 响应无 history 回显(6e7a137 新增字段缺失) |
| model | 48091 | 15:06 | 不受 T1~T5 影响 | — |
| evidence(current) | 48088 | 本次自启 | 当前构建 | history 回显正常、快照落库正常 |
| retrieval(current) | 48089 | 本次自启 | 当前构建 | 消歧正常(products=[X100 Pro]) |
| chat(current) | 48093 | 本次自启 | 当前构建 | 上下文注入 + context_summary 正常 |

### 各 MC 结果
| # | 用例 | 结果 | 证据 |
|---|---|---|---|
| MC-01 | 多轮追问("那换屏多少钱?" + 历史"X100 Pro 碎屏能免费修吗") | ✅ PASS | evidence 48088 evaluate-rpc: `answerable=true, answer=官方换屏一口价为¥699，含人工费；若购买了EW服务，则可享5折优惠。[C1][C2]`; chat 48093 端到端同答。确定性验证: retrieval 48089 search-rpc(带 history) `questionProducts=["X100 Pro"]`, answer 含 ¥699 |
| MC-02 | history 快照落库 | ✅ PASS | `ai_evidence_eval` id=80/81: `history=[{"role":"USER","content":"X100 Pro 碎屏能免费修吗"},{"role":"AI","content":"不能,屏幕碎裂属意外损坏,不在免费保修范围。"}]`(2 轮, 当前轮"那换屏多少钱?"未入快照); chat 链路 id=83/84/85 快照 2→3→5 轮随会话增长 |
| MC-03 | SYSTEM 不进上下文 | ✅ PASS(注: 验证口径修正) | ① chat 端到端: 会话 23 msg1→转人工(SYSTEM"会话已转人工: 测试转人工")→msg2, 落库快照(id=83)仅含 USER+AI 2 轮, **无 SYSTEM**; ② 单元级: ContextFormatter.formatHistory 输出不含 SYSTEM 轮(仅 [用户][客服]); ③ 代码: `selectRecentByConversationId` SQL `role IN ('USER','AI')` 直接排除 SYSTEM + ChatPipeline 二次过滤。**注意**: evaluate-rpc 的响应回显与落库快照是**原始输入**逐字透传(设计如此, 供审计), id=88 快照含 SYSTEM 属预期; 计划里"回显必须不含 SYSTEM"的口径与代码不符, 已按实际过滤点(提示词构造/chat 历史读取)验证 |
| MC-04 | 超长截断 | ✅ PASS | 12 轮 × 250 字(共 3000 字) evaluate-rpc → `code=0, answerable=true, answer=官方换屏一口价为¥699，PLUS 会员可额外减免¥100[C2]`, 无异常; 单元级 ContextFormatter: 输出 1890 字 ≤ 2000 总限, **第1轮(最旧)被丢弃、第12轮保留**, 单轮截断至 200 字+"…"; chat 侧 `yudao.chat.max-context-messages: 6` 绑定(ChatProperties prefix `yudao.chat`)生效, 快照轮数封顶 6 |
| MC-05 | 单轮兼容 | ✅ PASS | history=[] → `code=0, answerable=true`, 回答"X100 Pro 碎屏不能免费修…¥699"; history 缺省(null) → 同答, 快照 NULL |
| MC-06 | context_summary 落库 | ✅ PASS | chat 48093 会话 23: msg1"X100 Pro 碎屏能免费修吗"→转人工→`ai_conversation.context_summary="近轮对话要点: [用户] X100 Pro 碎屏能免费修吗 / [客服] X100 Pro 碎屏不能免费修。屏幕碎裂属于意外损坏，不在免费保修范围内 [C1]。意外损坏维修需自费，可选择 EW …"`, status=TRANSFERRED, SYSTEM 消息"会话已转人工: 测试转人工"已落 ai_message |

### 编译
`mvn -o -pl yudao-module-chat/yudao-module-chat-server,yudao-module-evidence/yudao-module-evidence-server,yudao-module-retrieval/yudao-module-retrieval-server -am compile -q -DskipTests` → **exit 0** ✅

### 结论与遗留
- 全链路多轮上下文(chat→evidence→retrieval 消歧→生成带历史→快照落库→context_summary)在当前构建上全部验证通过。
- **LB 混合观测(用户需重起)**: evidence 48088 的 evaluate 多次调用中约半数落到**旧 retrieval 48086**(无 history 消歧)返回 `refusal=产品不匹配`、半数落到当前 48089 正常作答 —— 与计划预判的 round-robin 完全一致; chat 的 evaluate 亦在旧 evidence 48087(history 快照=NULL)与当前 48088 之间轮询。**生产效果以用户重起 48086/48087/48083 为最终生效点**(IDEA 重新构建并运行), 重起前旧实例会静默丢弃 history。
- 无代码缺陷需要修复; 无一行后端代码改动。
- 验证用空闲端口实例(48088/48089/48093)验证完毕后已全部退出, 用户进程未受影响。
