# 对话工作台实施计划(7 任务: 接线/会话消息/管线/转人工/接口/前端/验证)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox syntax.

**Goal:** chat-server(48083)落地对话工作台:消息 → 复用证据平台判定 → 可作答带引用回答 / 不可作答自动转人工(带上下文摘要)+ 完整前端工作台(会话列表/消息流/AI建议+证据面板/一键采纳/转人工接管/会话记录),WEB 真实链路 + ChannelAdapter 渠道抽象,非流式。

**Architecture:** chat-server 编排;Feign 调 evidence.evaluate(复用完整判定管线)、model.chat(辅助);ai_conversation/ai_message 落库;TransferHandler 五类触发 + 交接摘要;前端改造 workbench-vben 接真实 API。

**Spec:** `docs/superpowers/specs/2026-08-18-对话工作台-design.md`

---

## 环境前提
- evidence-server(48087)已交付:`POST /admin-api/evidence/evaluate` 返回 answerable/confidence/refusalReason/evidence[]/conflicts[]/answer/claims/claimFail/traceId/elapsedMs
- chat-server(48083)骨架已有(send 占位返回固定串);gateway 路由 `/admin-api/chat/**` 已有
- DB 表 ai_conversation/ai_message 已有(需增量列);前端 workbench-vben(729 行 mock 页)可改造
- ⚠️ EvidenceApi 目前是占位(verifyClaim),**T1 必须补真实 RPC** 供 chat Feign 调用

## 任务

### Task 1: 接线 + evidence RPC + DDL 增量 + 配置块
- evidence-api: 补 `EvidenceEvaluateReqDTO{query, kbIds, topK, tenantId, userId}` + `EvidenceEvaluateRespDTO`(answerable/confidence/consultable/refusalReason/evidence[{chunkId,content,documentName,versionNo,score,channels}]/conflicts[{evidenceIndexA,B,reason}]/answer/claims[{text,verdict,evidenceIndex}]/claimFail/traceId/elapsedMs)
- evidence-api EvidenceApi: 加 `@PostMapping(ApiConstants.PREFIX + "/evaluate-rpc") CommonResult<EvidenceEvaluateRespDTO> evaluate(@RequestBody EvidenceEvaluateReqDTO req)`(保留 verifyClaim 占位或移除——移除更干净, 确认无引用)
- evidence-server EvidenceApiImpl: 改 @RestController 实现, 委托 EvidenceService.evaluate, 映射 RespVO→DTO(注意 EvidenceService.evaluate 从 SecurityFrameworkUtils 取 tenant/user——RPC 需显式传, 检查 EvidenceService 签名是否支持显式 tenantId/userId, 不支持则重载)
- chat-server pom: + evidence-api/model-api/knowledge-api(cn.iocoder.cloud ${revision});RpcConfiguration 登记 EvidenceApi/ModelApi/KnowledgeApi/ChatApi
- DDL(MySQL): `ALTER TABLE ai_conversation ADD COLUMN summary varchar(500) NULL COMMENT '转人工交接摘要', ADD COLUMN transfer_reason varchar(200) NULL COMMENT '转人工原因', ADD COLUMN operator_id bigint NULL COMMENT '接管坐席编号'`;`ALTER TABLE ai_message ADD COLUMN intent varchar(64) NULL COMMENT '意图', ADD COLUMN entities json NULL COMMENT '实体', ADD COLUMN confidence decimal(5,4) NULL COMMENT '充分度', ADD COLUMN trace_id varchar(64) NULL COMMENT '证据链路追踪号'`(MySQL 8 不支持 ADD COLUMN IF NOT EXISTS, 直接 ADD;若已存在先 SHOW COLUMNS 确认)
- application-local.yaml: `yudao.chat.transfer.keywords`(人工/客服/投诉/赔偿等) + `yudao.chat.channels.web.enabled: true` + `yudao.chat.max-context-messages: 6`
- 编译 + 启动验证(health + send 骨架仍可跑)

### Task 2: ConversationService + MessageService
- ConversationService: 创建会话(WEB 渠道, customerId 默认 "anonymous")/更新状态(状态机 ACTIVE→TRANSFERRED→CLOSED, take-over 后 TRANSFERRED→HANDLED?——沿用已有 status 枚举 ACTIVE/TRANSFERRED/CLOSED, 接管后置 CLOSED 加 operatorId 记录, 或加 HANDLED 枚举——选: 加 HANDLED 更清晰, 同步改 ai_conversation.status 注释)/分页查询(状态筛选/排序)/更新 intent
- MessageService: 落库 USER/AI/SYSTEM 消息(含 citations JSON/intent/entities/confidence/traceId)/查询会话消息列表(按 create_time)
- DO/Mapper: AiConversationDO/AiConversationMapper/AiMessageDO/AiMessageMapper(照 evidence-server dal 模式, TenantBaseDO + BaseMapperX)
- 注意: conversationId 为空建会话返回新 id;消息必带 conversation_id

### Task 3: ChatPipeline + EvidenceRpcAdapter
- EvidenceRpcAdapter: 包装 evidenceApi.evaluate, 从 SecurityFrameworkUtils.getLoginUser() 取 tenantId/userId(坐席/客户场景;WEB 匿名客户 → 取登录用户或 tenant 上下文, 与检索页一致), 失败降级: RPC 异常 → 返回 answerable=false + reason"评估服务暂不可用"(不抛出)
- ChatPipeline.send(conversationId, message, channel, customerId): 建/续会话 → 落 USER 消息 → 调 evaluate(topK=8, kbIds 空=全部可见——注意: 需先查可见知识库?证据平台内部已做权限, kbIds 传 null 走全部)→ 判定:
  - answerable && answer!=null && !claimFail → 落 AI 消息(含 citations=[evidence[].chunkId] 被 claims 引用的那些, 简化: 取 claims 引用的 evidenceIndex 对应 chunkId)+ 返回 reply
  - 否则 → 交给 TransferHandler(见 T4)
- 会话 intent 更新(取 evaluate 返回?evidence DTO 无 intent——用 model.chat 辅助分析?简化: 转人工关键词判断用规则, intent 落库用 evidence 的 questionProducts/或留空, T4 细化)

### Task 4: TransferHandler
- 触发规则(结构化, 可配置):
  1. answerable=false 且 refusalReason 含"证据不足/未检索/冲突/产品不匹配/阻断"(映射: 冲突→"证据冲突", 产品不匹配→"产品不匹配", 其他→"证据不足")
  2. claimFail=true → "Claim验证失败"
  3. 客户消息含配置关键词(人工/客服/投诉/赔偿/转人工)→ "客户要求"
  4. 情绪词表(太差/垃圾/滚/投诉/赔偿/起诉)→ "情绪激烈"(与 3 可重叠, 优先级 4>3)
- 执行: 会话置 TRANSFERRED + transferReason;组装 summary(原问题 + AI建议[answer 若非空附前 100 字] + 证据摘要[最多 3 条 documentName:versionNo] + 原因);落 SYSTEM 消息(内容=summary, 或单独字段);返回 transferRequired=true
- 手动: ConversationService.transfer(conversationId, reason)/takeOver(conversationId, operatorId)——controller 调, T5 接
- 留痕(BR-022): 采纳/驳回/接管 → ai_feedback 表(有表)或 ai_message SYSTEM 记录——T6 前端配合, 本任务先落接管/转人工的 SYSTEM 消息

### Task 5: Controller + VO + 权限 + 验证
- ChatController: `POST /chat/chat/send` `{conversationId?, message, channel?, customerId?}`(channel 默认 WEB)→ ChatSendRespVO{conversationId, reply, answerable, confidence, citations[], traceId, transferRequired, transferReason?, summary?}
- ConversationController: `POST /chat/conversation/transfer` `{conversationId, reason?}` / `POST /chat/conversation/take-over` `{conversationId}` / `GET /chat/conversation/history?conversationId=`(返回会话信息+消息列表)/ `GET /chat/conversation/page`(分页, 状态筛选)
- @PreAuthorize: chat:chat:send / chat:conversation:transfer / chat:conversation:take-over / chat:conversation:query
- 编译 + 启动验证: health + send("X100 Pro 碎屏能免费修吗")→ 真实回答带引用;send("苹果13 保修")→ transferRequired=true;DB 查 ai_message 落库

### Task 6: 前端对话工作台(改造 workbench-vben)
- api/ai/chat/index.ts: sendMessage/transferConversation/takeOverConversation/getHistory/getConversationPage
- 改造 views/ai/workbench-vben/index.vue(mock → 真实):
  - 会话列表: 调 /conversation/page, 状态徽标, TRANSFERRED 优先置顶, 新建会话
  - 消息流: USER/AI/SYSTEM 气泡;AI 消息引用角标([C1] 样式复用检索页)→ 点击弹证据原文(可调 /evidence/evaluate 结果缓存或后端 history 返回 citations 对应内容——后端 history 消息附 citations 内容?简化: 消息 citations 存 chunkId, 前端调 ingestion chunk get 接口拿原文, 或后端 history 联表——T6 决定, 优先后端 history 返回含 citations 的 evidence 详情)
  - AI 建议卡片: answerable/confidence/意图/证据列表(复用证据评估面板样式)
  - 一键采纳按钮(采纳后 message.success + 记录)
  - 转人工按钮(手动 transfer)+ TRANSFERRED 会话交接摘要卡片 + 接管按钮
  - 会话记录完整可查
- 菜单: 工作台菜单已有(6736 AI 对话工作台), 无需新增
- typecheck + 提交

### Task 7: 端到端验证
- AC-01: send("X100 Pro 碎屏能免费修吗")→ 回答带 [C1], citations 落库, ai_message 完整
- AC-02: send("苹果13 保修")→ transferRequired=true, transferReason=产品不匹配, summary 含问题+原因
- AC-03: send("我要转人工")→ 关键词触发, 会话 TRANSFERRED
- AC-04: history 查看 TRANSFERRED 会话 → 摘要可见 → take-over → 状态更新
- AC-05: 全程落库可查(ai_conversation status + ai_message role/content/citations)
- AC-06: 渠道抽象生效, 企微/钉钉未注册不报错
- 前端: 工作台列表/消息/采纳/转人工/接管全流程人工点验

---

## 自审记录
- 非流式: 完全复用 evidence.evaluate, 判定/Claim 验证不重做, 链路最短
- 领域无关: 渠道/会话/转人工全抽象, 关键词/阈值 yaml 可配
- 降级: evidence RPC 异常 → answerable=false + reason, 不抛出;渠道未注册跳过
- 落库: ai_conversation(状态机+summary/reason)+ ai_message(USER/AI/SYSTEM + citations/intent/confidence/trace)
- 留痕: 转人工/接管/采纳均落库(BR-022), 供评测/审计
