# AI 文档入库管线(切分 + 向量化 + 三写)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 上传的文档由 ingestion-server 异步消费,完成 解析 → 五策略切分 → BGE-M3 向量化 → MySQL/Milvus/ES 三写,`ai_document.parse_status` 流转到 INDEXED。

**Architecture:** knowledge-server 上传登记后发 Kafka 消息(`knowledge-ingest`);ingestion-server 消费消息,用策略模式解析(Tika/PDFBox/POI/Tesseract OCR)和切分(五策略),经 Feign 调 model-server 的 embedding 接口(LM Studio BGE-M3),最后三写 MySQL ai_chunk + Milvus 向量 + ES BM25,再回写解析状态。

**Tech Stack:** Spring Boot 3.5 / spring-kafka / Apache Tika 3.2.3 / PDFBox 3.0.3 / POI 4.1.2 / milvus-sdk-java 2.4.1 / elasticsearch-rest-client 8.13.4 / MyBatis-Plus / LM Studio(text-embedding-bge-m3, 127.0.0.1:1234)

**Spec:** `docs/9-入库管线设计.md`

---

## 环境前提(已就绪,勿重复)

- Docker 中间件 10/10 healthy(MySQL 3306 / Redis 6379 / Nacos 8848 / MinIO 9000 / ES 9200 / Kafka 9092 / Milvus 19530),compose 在 `deploy/yudao-cloud-dev/docker-compose.yml`
- Kafka 主题 `knowledge-ingest` 已创建(3 分区)
- LM Studio 运行中:`http://127.0.0.1:1234/v1/embeddings`,模型 `text-embedding-bge-m3`,1024 维(已 curl 验证)
- 后端已启动:gateway(48080)、system(48081)、infra(48082)、knowledge-server(48084)、model-server(48091)
- ingestion-server(48085)未启动,本计划第 8 步启动它
- 前端 dev server 5666 运行中(yudao-ui-admin-vben)

## 文件结构总览

```
yudao-module-knowledge/
  yudao-module-knowledge-api/src/main/java/cn/iocoder/yudao/module/knowledge/api/
    KnowledgeApi.java                          [修改] 新增 updateParseStatus
  yudao-module-knowledge-server/
    pom.xml                                    [修改] + spring-kafka
    src/main/resources/sql/knowledge.sql       [修改] ai_document + error_msg 字段
    src/main/java/.../service/knowledge/impl/AiDocumentServiceImpl.java  [修改] 发 Kafka
    src/main/java/.../mq/KnowledgeIngestProducer.java        [新增] Kafka 生产者
    src/main/java/.../api/KnowledgeApiImpl.java              [修改] 实现 updateParseStatus
    src/main/java/.../service/knowledge/AiDocumentService.java [修改] 接口 + updateParseStatus
    src/main/java/.../dal/dataobject/knowledge/AiDocumentDO.java [修改] + errorMsg
    src/main/java/.../dal/mysql/knowledge/AiDocumentMapper.java  [修改] + updateParseStatus

yudao-module-model/
  yudao-module-model-api/src/main/java/cn/iocoder/yudao/module/model/api/
    ModelApi.java                              [修改] + embedding(List<String>) → List<List<Float>>
    dto/EmbeddingRespDTO.java                  [新增]
  yudao-module-model-server/
    pom.xml                                    [修改] + spring-web(已有) 无新依赖
    src/main/java/.../api/ModelApiImpl.java    [修改] 实现 embedding(HTTP 调 LM Studio)

yudao-module-ingestion/
  yudao-module-ingestion-server/
    pom.xml                                    [修改] + tika/pdfbox/poi/milvus/es-rest-client/spring-kafka
    src/main/resources/application-local.yaml  [修改] + embedding 配置(model-server url)
    src/main/java/cn/iocoder/yudao/module/ingestion/
      mq/KnowledgeIngestConsumer.java          [新增] @KafkaListener 消费入口
      parse/DocumentParser.java                [新增] 接口
      parse/TextParser.java                    [新增] TXT/MD
      parse/PdfParser.java                     [新增] PDF(PDFBox + OCR 兜底)
      parse/OfficeParser.java                  [新增] Word/Excel/PPT(Tika + POI)
      split/ChunkSplitter.java                 [新增] 接口(入参 文本+策略 → List<Chunk>)
      split/SemanticSplitter.java              [新增]
      split/ParentChildSplitter.java           [新增]
      split/TableSplitter.java                 [新增]
      split/FaqSplitter.java                   [新增]
      split/PolicySplitter.java                [新增]
      split/SplitterFactory.java               [新增] 策略选择器
      embedding/EmbeddingClient.java           [新增] Feign 调 model-server
      store/ChunkStore.java                    [新增] 三写编排
      store/MysqlChunkStore.java               [新增]
      store/MilvusChunkStore.java              [新增]
      store/EsChunkStore.java                  [新增]
      service/IngestService.java               [新增] 主流程
      service/IngestServiceImpl.java           [新增]
      dal/dataobject/ChunkDO.java              [新增]
      dal/mysql/ChunkMapper.java               [新增]

yudao-ui-admin-vben(apps/web-antd/src/views/ai/)
  knowledge/data.ts                            [修改] 切分策略 5 种
  document/index.vue                           [修改] 失败显示 errorMsg
```

---

### Task 1: knowledge-server — ai_document 加 error_msg 字段

**Files:**
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/resources/sql/knowledge.sql`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/java/cn/iocoder/yudao/module/knowledge/dal/dataobject/knowledge/AiDocumentDO.java`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/java/cn/iocoder/yudao/module/knowledge/dal/mysql/knowledge/AiDocumentMapper.java`

- [ ] **Step 1: 更新 DDL,给 ai_document 加 error_msg 字段**

在 `knowledge.sql` 的 `ai_document` 表 `parse_status` 行后追加:

```sql
  `error_msg` varchar(512) CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci NULL DEFAULT NULL COMMENT '失败原因',
```

并在文件末尾追加增量语句(已建库环境执行):

```sql
ALTER TABLE `ai_document` ADD COLUMN IF NOT EXISTS `error_msg` varchar(512) NULL DEFAULT NULL COMMENT '失败原因' AFTER `parse_status`;
```

- [ ] **Step 2: AiDocumentDO 加字段**

在 `AiDocumentDO.java` 的 `parseStatus` 字段后加:

```java
    /** 失败原因 */
    private String errorMsg;
```

- [ ] **Step 3: AiDocumentMapper 加更新状态方法**

```java
    default int updateParseStatus(Long id, String parseStatus, String errorMsg) {
        return update(null, new LambdaUpdateWrapper<AiDocumentDO>()
                .eq(AiDocumentDO::getId, id)
                .set(AiDocumentDO::getParseStatus, parseStatus)
                .set(AiDocumentDO::getErrorMsg, errorMsg));
    }
```

> 检查 `AiDocumentMapper.java` 现有 import,确认已引入 `com.baomidou.mybatisplus.core.conditions.update.LambdaUpdateWrapper`。

- [ ] **Step 4: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-knowledge/yudao-module-knowledge-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 5: 提交**

```bash
git add yudao-module-knowledge
git commit -m "feat(knowledge): ai_document 增加 error_msg 字段与状态更新方法"
```

---

### Task 2: knowledge-server — 上传后发 Kafka 消息

**Files:**
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/pom.xml`
- Create: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/java/cn/iocoder/yudao/module/knowledge/mq/KnowledgeIngestProducer.java`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/java/cn/iocoder/yudao/module/knowledge/service/knowledge/impl/AiDocumentServiceImpl.java`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/resources/application-local.yaml`(注释已配 kafka)

- [ ] **Step 1: pom 加 spring-kafka 依赖**

在 `pom.xml` 的 `<dependencies>` 中追加:

```xml
        <!-- 消息队列 Kafka -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
```

- [ ] **Step 2: 新建 Kafka 生产者**

`mq/KnowledgeIngestProducer.java`:

```java
package cn.iocoder.yudao.module.knowledge.mq;

import org.springframework.kafka.core.KafkaTemplate;
import org.springframework.stereotype.Component;
import jakarta.annotation.Resource;

@Component
public class KnowledgeIngestProducer {

    public static final String TOPIC_KNOWLEDGE_INGEST = "knowledge-ingest";

    @Resource
    private KafkaTemplate<String, Object> kafkaTemplate;

    /** 发送入库任务消息, payload 为文档编号 */
    public void sendDocumentIngest(Long documentId) {
        kafkaTemplate.send(TOPIC_KNOWLEDGE_INGEST, String.valueOf(documentId), documentId);
    }

}
```

- [ ] **Step 3: 上传登记成功后发消息**

在 `AiDocumentServiceImpl.createAiDocument` 的 `aiDocumentMapper.insert(doc)` 之后、`return doc.getId()` 之前插入:

```java
        // 发送入库任务消息(Kafka), ingestion-server 异步解析/切分/向量化/三写
        knowledgeIngestProducer.sendDocumentIngest(doc.getId());
```

并在类上增加字段:

```java
    @Resource
    private KnowledgeIngestProducer knowledgeIngestProducer;
```

并补 import:`import cn.iocoder.yudao.module.knowledge.mq.KnowledgeIngestProducer;`

- [ ] **Step 4: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-knowledge/yudao-module-knowledge-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 5: 提交**

```bash
git add yudao-module-knowledge
git commit -m "feat(knowledge): 文档上传登记后发送 Kafka 入库消息"
```

---

### Task 3: knowledge-server — 新增解析状态回写 Feign 接口

**Files:**
- Modify: `yudao-module-knowledge/yudao-module-knowledge-api/src/main/java/cn/iocoder/yudao/module/knowledge/api/KnowledgeApi.java`
- Create: `yudao-module-knowledge/yudao-module-knowledge-api/src/main/java/cn/iocoder/yudao/module/knowledge/api/dto/ParseStatusUpdateReqDTO.java`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/java/cn/iocoder/yudao/module/knowledge/api/KnowledgeApiImpl.java`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/src/main/java/cn/iocoder/yudao/module/knowledge/service/knowledge/AiDocumentService.java`

- [ ] **Step 1: API 接口加回写方法**

`KnowledgeApi.java` 增加:

```java
    /**
     * 更新文档解析状态(ingestion-server 回写)
     *
     * @param documentId 文档编号
     * @param parseStatus 解析状态: PARSING / EMBEDDING / INDEXED / FAILED
     * @param errorMsg 失败原因(成功传 null)
     */
    Boolean updateDocumentParseStatus(Long documentId, String parseStatus, String errorMsg);
```

- [ ] **Step 2: 新建 DTO(如需传对象则用;此处参数简单,可直接用 @RequestParam)**

> 决策:Feign 接口参数用简单类型 + `@RequestParam` 注解,避免 DTO。保持 `KnowledgeApi.java` 如下:

```java
    @GetMapping("/update-parse-status")
    Boolean updateDocumentParseStatus(@RequestParam("documentId") Long documentId,
                                      @RequestParam("parseStatus") String parseStatus,
                                      @RequestParam(value = "errorMsg", required = false) String errorMsg);
```

> 注意:芋道 Feign 接口默认用 `@FeignClient`,方法路径基于服务上下文。为保持与芋道惯例一致,方法上使用 `org.springframework.web.bind.annotation.GetMapping` / `RequestParam`。

- [ ] **Step 3: Service 接口加方法**

`AiDocumentService.java` 增加:

```java
    /**
     * 更新文档解析状态
     */
    void updateParseStatus(Long id, String parseStatus, String errorMsg);
```

- [ ] **Step 4: Service 实现**

`AiDocumentServiceImpl.java` 增加:

```java
    @Override
    public void updateParseStatus(Long id, String parseStatus, String errorMsg) {
        validateAiDocumentExists(id);
        aiDocumentMapper.updateParseStatus(id, parseStatus, errorMsg);
    }
```

- [ ] **Step 5: Feign 实现**

`KnowledgeApiImpl.java` 增加:

```java
    @Override
    public Boolean updateDocumentParseStatus(Long documentId, String parseStatus, String errorMsg) {
        aiDocumentService.updateParseStatus(documentId, parseStatus, errorMsg);
        return true;
    }
```

> 检查 `KnowledgeApiImpl.java` 现有结构,补 `@Resource AiDocumentService` 与 import。

- [ ] **Step 6: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-knowledge/yudao-module-knowledge-api,yudao-module-knowledge/yudao-module-knowledge-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 7: 提交**

```bash
git add yudao-module-knowledge
git commit -m "feat(knowledge): 新增文档解析状态回写 Feign 接口"
```

---

### Task 4: model-server — 新增 Embedding Feign 接口

**Files:**
- Modify: `yudao-module-model/yudao-module-model-api/src/main/java/cn/iocoder/yudao/module/model/api/ModelApi.java`
- Modify: `yudao-module-model/yudao-module-model-server/src/main/java/cn/iocoder/yudao/module/model/api/ModelApiImpl.java`
- Modify: `yudao-module-model/yudao-module-model-server/src/main/resources/application-local.yaml`

- [ ] **Step 1: API 接口加 embedding 方法**

`ModelApi.java` 增加:

```java
    /**
     * 文本向量化(Embedding)
     *
     * @param texts 文本列表(批量)
     * @return 每个文本对应的 1024 维向量列表
     */
    @PostMapping("/embedding")
    List<List<Float>> embedding(@RequestBody List<String> texts);
```

> 需补 import:`org.springframework.web.bind.annotation.PostMapping` / `RequestBody` / `java.util.List`。

- [ ] **Step 2: 实现类加 embedding(HTTP 调 LM Studio)**

`ModelApiImpl.java` 增加:

```java
    @Override
    public List<List<Float>> embedding(List<String> texts) {
        // 读取本地配置的 Embedding 服务(LM Studio, OpenAI 兼容接口)
        String baseUrl = environment.getProperty("yudao.model.embedding.base-url", "http://127.0.0.1:1234/v1");
        String modelName = environment.getProperty("yudao.model.embedding.model", "text-embedding-bge-m3");
        String url = baseUrl + "/embeddings";

        Map<String, Object> body = new HashMap<>();
        body.put("model", modelName);
        body.put("input", texts);

        HttpHeaders headers = new HttpHeaders();
        headers.setContentType(MediaType.APPLICATION_JSON);
        HttpEntity<Map<String, Object>> request = new HttpEntity<>(body, headers);

        // 使用 Spring RestTemplate 调用(芋道 common 已有 RestTemplate 或直接 new)
        RestTemplate restTemplate = new RestTemplate();
        ResponseEntity<Map> response = restTemplate.postForEntity(url, request, Map.class);
        List<Map<String, Object>> data = (List<Map<String, Object>>) response.getBody().get("data");
        List<List<Float>> result = new ArrayList<>();
        for (Map<String, Object> item : data) {
            result.add((List<Float>) item.get("embedding"));
        }
        return result;
    }
```

> 需补 import:`org.springframework.core.env.Environment`(字段注入)、`org.springframework.http.*`、`org.springframework.web.client.RestTemplate`、`java.util.*`。
> 类上增加:`@Resource private Environment environment;`

- [ ] **Step 3: application-local.yaml 加 embedding 配置**

```yaml
yudao:
  model:
    embedding:
      base-url: http://127.0.0.1:1234/v1   # LM Studio
      model: text-embedding-bge-m3
```

- [ ] **Step 4: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-model/yudao-module-model-api,yudao-module-model/yudao-module-model-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 5: 提交**

```bash
git add yudao-module-model
git commit -m "feat(model): 新增 embedding 向量化 Feign 接口(LM Studio BGE-M3)"
```

---

### Task 5: ingestion-server — pom 依赖 + 配置

**Files:**
- Modify: `yudao-module-ingestion/yudao-module-ingestion-server/pom.xml`
- Modify: `yudao-module-ingestion/yudao-module-ingestion-server/src/main/resources/application-local.yaml`

- [ ] **Step 1: pom 加依赖**

```xml
        <!-- 文档解析: Tika(含 OCR)+ PDFBox + POI -->
        <dependency>
            <groupId>org.apache.tika</groupId>
            <artifactId>tika-parsers-standard-package</artifactId>
            <version>3.2.3</version>
        </dependency>
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.3</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-ooxml</artifactId>
            <version>4.1.2</version>
        </dependency>
        <dependency>
            <groupId>org.apache.poi</groupId>
            <artifactId>poi-scratchpad</artifactId>
            <version>4.1.2</version>
        </dependency>
        <!-- 向量库 Milvus -->
        <dependency>
            <groupId>io.milvus</groupId>
            <artifactId>milvus-sdk-java</artifactId>
            <version>2.4.1</version>
        </dependency>
        <!-- Elasticsearch -->
        <dependency>
            <groupId>org.elasticsearch.client</groupId>
            <artifactId>elasticsearch-rest-client</artifactId>
            <version>8.13.4</version>
        </dependency>
        <!-- 消息队列 Kafka -->
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
        </dependency>
        <!-- Feign 调用 model-server -->
        <dependency>
            <groupId>cn.iocoder.cloud</groupId>
            <artifactId>yudao-module-model-api</artifactId>
        </dependency>
        <dependency>
            <groupId>cn.iocoder.cloud</groupId>
            <artifactId>yudao-module-knowledge-api</artifactId>
        </dependency>
```

> 注意:若 `yudao-module-ingestion-server` 依赖 `yudao-spring-boot-starter-rpc`(已有),Feign 客户端会自动扫描 `@FeignClient`。确认 api 模块的 groupId/artifactId 与 knowledge/model 的 api 模块一致(检查父 pom modules 与 dependencyManagement)。

- [ ] **Step 2: application-local.yaml 加 milvus/embedding 配置**

在现有 `yudao.ai.milvus` 下补充(现有文件已有 host/port,确认保留),并新增 embedding 与 es 索引配置:

```yaml
yudao:
  ai:
    milvus:
      host: 127.0.0.1
      port: 19530
      collection: ai_chunk_vector   # 向量集合名
      dim: 1024                     # BGE-M3 向量维度
    es:
      index: ai_chunk_bm25          # BM25 索引名
```

> `spring.elasticsearch.uris` 已在 application-local.yaml(现有)。

- [ ] **Step 3: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-ingestion/yudao-module-ingestion-server -am compile -q -DskipTests
```

> 若 milvus-sdk 传递依赖冲突(如 grpc 版本),在 ingestion-server pom 中加 `<exclusions>` 排除。首次下载依赖可能较慢。

Expected: BUILD SUCCESS

- [ ] **Step 4: 提交**

```bash
git add yudao-module-ingestion
git commit -m "feat(ingestion): 引入解析/向量/ES/Kafka 依赖与配置"
```

---

### Task 6: ingestion-server — 文档解析(策略模式)

**Files:**
- Create: `.../ingestion/parse/DocumentParser.java`
- Create: `.../ingestion/parse/TextParser.java`
- Create: `.../ingestion/parse/PdfParser.java`
- Create: `.../ingestion/parse/OfficeParser.java`

- [ ] **Step 1: 接口**

`parse/DocumentParser.java`:

```java
package cn.iocoder.yudao.module.ingestion.parse;

/**
 * 文档解析器: 把原始文档解析为纯文本
 */
public interface DocumentParser {

    /**
     * 解析文档内容
     *
     * @param filePath 文档路径(MinIO 下载后的本地临时文件)
     * @param docType 文档类型: TXT/MD/PDF/WORD/EXCEL/PPT/IMAGE
     * @return 解析出的纯文本
     */
    String parse(String filePath, String docType) throws Exception;

}
```

- [ ] **Step 2: TextParser(TXT/MD)**

`parse/TextParser.java`:

```java
package cn.iocoder.yudao.module.ingestion.parse;

import cn.hutool.core.io.FileUtil;
import org.springframework.stereotype.Component;

import java.nio.charset.StandardCharsets;

/**
 * TXT / MD 纯文本解析
 */
@Component
public class TextParser implements DocumentParser {

    @Override
    public String parse(String filePath, String docType) {
        return FileUtil.readString(filePath, StandardCharsets.UTF_8);
    }

}
```

- [ ] **Step 3: PdfParser(PDF,含 OCR 兜底)**

`parse/PdfParser.java`:

```java
package cn.iocoder.yudao.module.ingestion.parse;

import org.apache.pdfbox.Loader;
import org.apache.pdfbox.pdmodel.PDDocument;
import org.apache.pdfbox.text.PDFTextStripper;
import org.springframework.stereotype.Component;

import java.io.File;

/**
 * PDF 解析: 优先提取文本层;无文本层(扫描件)由调用方走 OCR
 */
@Component
public class PdfParser implements DocumentParser {

    @Override
    public String parse(String filePath, String docType) throws Exception {
        try (PDDocument document = Loader.loadPDF(new File(filePath))) {
            PDFTextStripper stripper = new PDFTextStripper();
            String text = stripper.getText(document);
            // 若提取不到有效文本, 返回空, 由上层决定走 OCR
            return text == null ? "" : text.trim();
        }
    }

}
```

- [ ] **Step 4: OfficeParser(Word/Excel/PPT,经 Tika)**

`parse/OfficeParser.java`:

```java
package cn.iocoder.yudao.module.ingestion.parse;

import org.apache.tika.Tika;
import org.apache.tika.exception.TikaException;
import org.springframework.stereotype.Component;

import java.io.File;
import java.io.IOException;

/**
 * Word/Excel/PPT 解析(经 Apache Tika, 内部使用 POI)
 */
@Component
public class OfficeParser implements DocumentParser {

    private final Tika tika = new Tika();

    @Override
    public String parse(String filePath, String docType) throws IOException {
        return tika.parseToString(new File(filePath));
    }

}
```

- [ ] **Step 5: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-ingestion/yudao-module-ingestion-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 6: 提交**

```bash
git add yudao-module-ingestion
git commit -m "feat(ingestion): 文档解析器(TXT/MD/PDF/Office)"
```

---

### Task 7: ingestion-server — 五策略切分

**Files:**
- Create: `.../ingestion/split/ChunkSplitter.java`
- Create: `.../ingestion/split/Chunk.java`(若复用 knowledge 的 ChunkDO 则跳过)
- Create: `.../ingestion/split/SemanticSplitter.java`
- Create: `.../ingestion/split/ParentChildSplitter.java`
- Create: `.../ingestion/split/TableSplitter.java`
- Create: `.../ingestion/split/FaqSplitter.java`
- Create: `.../ingestion/split/PolicySplitter.java`
- Create: `.../ingestion/split/SplitterFactory.java`

- [ ] **Step 1: Chunk 模型**

`split/Chunk.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import lombok.Data;

import java.util.ArrayList;
import java.util.List;

/**
 * 切分产物: 一个知识片段
 */
@Data
public class Chunk {

    /** 内容 */
    private String content;

    /** 类型: SEMANTIC/TABLE/FAQ/POLICY */
    private String chunkType;

    /** 父块编号(仅 ParentChild 策略的子块有值) */
    private Long parentId;

    /** 元数据(JSON 字符串) */
    private String metadata;

    public Chunk() {
    }

    public Chunk(String content, String chunkType) {
        this.content = content;
        this.chunkType = chunkType;
    }

    public static List<Chunk> of(String content, String chunkType) {
        List<Chunk> list = new ArrayList<>();
        list.add(new Chunk(content, chunkType));
        return list;
    }

}
```

- [ ] **Step 2: 接口**

`split/ChunkSplitter.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import java.util.List;

/**
 * 切分器: 把全文按策略切成片段
 */
public interface ChunkSplitter {

    /**
     * 切分文本
     *
     * @param text 全文
     * @param maxTokens 单块最大 token 数(默认 500)
     * @return 片段列表
     */
    List<Chunk> split(String text, int maxTokens);

}
```

- [ ] **Step 3: SemanticSplitter(按段落+标题)**

`split/SemanticSplitter.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

/**
 * 语义切分: 按空行分段, 超长段按句号/逗号再切, 控制单块 ≤ maxTokens
 */
@Component
public class SemanticSplitter implements ChunkSplitter {

    @Override
    public List<Chunk> split(String text, int maxTokens) {
        List<Chunk> result = new ArrayList<>();
        if (text == null || text.isBlank()) {
            return result;
        }
        // 1. 按空行切大段
        String[] paragraphs = text.split("\\n\\s*\\n");
        for (String para : paragraphs) {
            String trimmed = para.trim();
            if (trimmed.isEmpty()) {
                continue;
            }
            // 2. 超长段按句子切
            if (estimateTokens(trimmed) > maxTokens) {
                for (String sentence : splitLongParagraph(trimmed, maxTokens)) {
                    result.add(new Chunk(sentence, "SEMANTIC"));
                }
            } else {
                result.add(new Chunk(trimmed, "SEMANTIC"));
            }
        }
        return result;
    }

    /** 粗略估计 token 数: 中文按 1.5 字符/token, 英文按 4 字符/token */
    private int estimateTokens(String text) {
        return (int) Math.ceil(text.length() / 1.5);
    }

    /** 长段按句号切, 合并到 ≤ maxTokens */
    private List<String> splitLongParagraph(String para, int maxTokens) {
        List<String> sentences = new ArrayList<>();
        StringBuilder current = new StringBuilder();
        for (String sentence : para.split("(?<=[。！？.!?])")) {
            if (estimateTokens(current.toString()) + estimateTokens(sentence) > maxTokens
                    && current.length() > 0) {
                sentences.add(current.toString().trim());
                current.setLength(0);
            }
            current.append(sentence);
        }
        if (current.length() > 0) {
            sentences.add(current.toString().trim());
        }
        return sentences;
    }

}
```

- [ ] **Step 4: ParentChildSplitter**

`split/ParentChildSplitter.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

/**
 * 父子切分: 父块 = 大段落(≤ maxTokens*4); 子块 = 父块内按句切(≤ maxTokens), 子块 parentId 指向父块
 */
@Component
public class ParentChildSplitter implements ChunkSplitter {

    private final SemanticSplitter semanticSplitter = new SemanticSplitter();

    @Override
    public List<Chunk> split(String text, int maxTokens) {
        List<Chunk> result = new ArrayList<>();
        if (text == null || text.isBlank()) {
            return result;
        }
        // 1. 先按段落粗切(复用语义切分的段落边界逻辑)
        String[] paragraphs = text.split("\\n\\s*\\n");
        long parentSeq = 0;
        for (String para : paragraphs) {
            String trimmed = para.trim();
            if (trimmed.isEmpty()) {
                continue;
            }
            // 父块
            parentSeq++;
            Chunk parent = new Chunk(trimmed, "SEMANTIC");
            parent.setMetadata("{\"parent\":true}");
            result.add(parent);
            // 2. 子块: 父块超长时切子块, 引用父块 index
            if (estimateTokens(trimmed) > maxTokens) {
                List<String> subChunks = splitBySentences(trimmed, maxTokens);
                for (String sub : subChunks) {
                    Chunk child = new Chunk(sub, "SEMANTIC");
                    child.setParentId(parentSeq);
                    child.setMetadata("{\"parent\":false}");
                    result.add(child);
                }
            }
        }
        return result;
    }

    private int estimateTokens(String text) {
        return (int) Math.ceil(text.length() / 1.5);
    }

    private List<String> splitBySentences(String para, int maxTokens) {
        List<String> sentences = new ArrayList<>();
        StringBuilder current = new StringBuilder();
        for (String sentence : para.split("(?<=[。！？.!?])")) {
            if (estimateTokens(current.toString()) + estimateTokens(sentence) > maxTokens
                    && current.length() > 0) {
                sentences.add(current.toString().trim());
                current.setLength(0);
            }
            current.append(sentence);
        }
        if (current.length() > 0) {
            sentences.add(current.toString().trim());
        }
        return sentences;
    }

}
```

> 说明: 父块也入库(metadata parent=true), 子块 parentId 指向父块顺序号; 检索阶段优先用子块、上下文取父块。parentId 在入库时映射为父块在 MySQL 的真实 id(Task 10 处理)。

- [ ] **Step 5: TableSplitter**

`split/TableSplitter.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

/**
 * 表格切分: 识别行(按换行), 每行一个 chunk, 表头(第一行)拼入每行内容
 */
@Component
public class TableSplitter implements ChunkSplitter {

    @Override
    public List<Chunk> split(String text, int maxTokens) {
        List<Chunk> result = new ArrayList<>();
        if (text == null || text.isBlank()) {
            return result;
        }
        String[] lines = text.split("\\r?\\n");
        if (lines.length == 0) {
            return result;
        }
        String header = lines[0].trim(); // 表头
        for (int i = 1; i < lines.length; i++) {
            String line = lines[i].trim();
            if (line.isEmpty()) {
                continue;
            }
            String content = header.isEmpty() ? line : "表头: " + header + " | 行: " + line;
            result.add(new Chunk(content, "TABLE"));
        }
        return result;
    }

}
```

- [ ] **Step 6: FaqSplitter**

`split/FaqSplitter.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

/**
 * FAQ 切分: 识别 "问/答" 或 "Q/A" 对
 */
@Component
public class FaqSplitter implements ChunkSplitter {

    private static final Pattern FAQ_PATTERN = Pattern.compile(
            "(?:问|Q)\\s*[:：]\\s*(.+?)(?:答|A)\\s*[:：]\\s*(.+?)(?=(?:问|Q)\\s*[:：]|$)",
            Pattern.DOTALL);

    @Override
    public List<Chunk> split(String text, int maxTokens) {
        List<Chunk> result = new ArrayList<>();
        if (text == null || text.isBlank()) {
            return result;
        }
        Matcher matcher = FAQ_PATTERN.matcher(text);
        while (matcher.find()) {
            String question = matcher.group(1).trim();
            String answer = matcher.group(2).trim();
            result.add(new Chunk("问: " + question + "\n答: " + answer, "FAQ"));
        }
        // 无 FAQ 结构时, 退化为语义切分
        if (result.isEmpty()) {
            SemanticSplitter fallback = new SemanticSplitter();
            result.addAll(fallback.split(text, maxTokens));
        }
        return result;
    }

}
```

- [ ] **Step 7: PolicySplitter**

`split/PolicySplitter.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

/**
 * 条款切分: 按 "第X条" 或 "X.X" 或 "一、二、三、" 编号切分
 */
@Component
public class PolicySplitter implements ChunkSplitter {

    private static final Pattern POLICY_PATTERN = Pattern.compile(
            "((?:第[一二三四五六七八九十百0-9]+条|[0-9]+\\.[0-9]+|[一二三四五六七八九十]+、))");

    @Override
    public List<Chunk> split(String text, int maxTokens) {
        List<Chunk> result = new ArrayList<>();
        if (text == null || text.isBlank()) {
            return result;
        }
        Matcher matcher = POLICY_PATTERN.matcher(text);
        int lastEnd = 0;
        String lastTitle = "";
        while (matcher.find()) {
            if (matcher.start() > lastEnd) {
                String body = text.substring(lastEnd, matcher.start()).trim();
                if (!body.isEmpty()) {
                    result.add(new Chunk((lastTitle + " " + body).trim(), "POLICY"));
                }
            }
            lastTitle = matcher.group(1).trim();
            lastEnd = matcher.end();
        }
        if (lastEnd < text.length()) {
            String body = text.substring(lastEnd).trim();
            if (!body.isEmpty()) {
                result.add(new Chunk((lastTitle + " " + body).trim(), "POLICY"));
            }
        }
        return result;
    }

}
```

- [ ] **Step 8: SplitterFactory**

`split/SplitterFactory.java`:

```java
package cn.iocoder.yudao.module.ingestion.split;

import cn.hutool.core.util.StrUtil;
import org.springframework.stereotype.Component;

import java.util.HashMap;
import java.util.Map;

/**
 * 切分策略选择器(按 ai_knowledge_base.chunk_strategy)
 */
@Component
public class SplitterFactory {

    private final Map<String, ChunkSplitter> splitters = new HashMap<>();

    public SplitterFactory(SemanticSplitter semanticSplitter,
                           ParentChildSplitter parentChildSplitter,
                           TableSplitter tableSplitter,
                           FaqSplitter faqSplitter,
                           PolicySplitter policySplitter) {
        splitters.put("Semantic", semanticSplitter);
        splitters.put("ParentChild", parentChildSplitter);
        splitters.put("Table", tableSplitter);
        splitters.put("FAQ", faqSplitter);
        splitters.put("Policy", policySplitter);
    }

    /**
     * 按策略名获取切分器, 未知策略回退 Semantic
     */
    public ChunkSplitter getSplitter(String strategy) {
        if (StrUtil.isBlank(strategy)) {
            return splitters.get("Semantic");
        }
        return splitters.getOrDefault(strategy, splitters.get("Semantic"));
    }

}
```

- [ ] **Step 9: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-ingestion/yudao-module-ingestion-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 10: 提交**

```bash
git add yudao-module-ingestion
git commit -m "feat(ingestion): 五策略切分器(Semantic/ParentChild/Table/FAQ/Policy)"
```

---

### Task 8: ingestion-server — 三写存储(MySQL + Milvus + ES)

**Files:**
- Create: `.../ingestion/dal/dataobject/ChunkDO.java`
- Create: `.../ingestion/dal/mysql/ChunkMapper.java`
- Create: `.../ingestion/store/ChunkStore.java`
- Create: `.../ingestion/store/MysqlChunkStore.java`
- Create: `.../ingestion/store/MilvusChunkStore.java`
- Create: `.../ingestion/store/EsChunkStore.java`

- [ ] **Step 1: ChunkDO**

`dal/dataobject/ChunkDO.java`:

```java
package cn.iocoder.yudao.module.ingestion.dal.dataobject;

import cn.iocoder.yudao.framework.mybatis.core.dataobject.BaseDO;
import com.baomidou.mybatisplus.annotation.TableName;
import lombok.Data;
import lombok.EqualsAndHashCode;

/**
 * AI 知识片段(与 ai_chunk 表对应)
 */
@TableName("ai_chunk")
@Data
@EqualsAndHashCode(callSuper = true)
public class ChunkDO extends BaseDO {

    /** 编号 */
    private Long id;

    /** 版本编号(暂用文档 id 占位, 版本状态机后续接入) */
    private Long versionId;

    /** 片段内容 */
    private String content;

    /** 类型: SEMANTIC/TABLE/FAQ/POLICY */
    private String chunkType;

    /** 元数据 */
    private String metadata;

    /** 状态 */
    private String status;

    /** Milvus 向量关联键 */
    private String vectorKey;

    /** 父块编号 */
    private Long parentId;

}
```

> 注意: `ai_chunk` 表当前无 `parent_id` 字段, 需在 Task 8 的 DDL 补充(或 Task 1 一并加)。在 `knowledge.sql` 与 MySQL 执行:

```sql
ALTER TABLE `ai_chunk` ADD COLUMN IF NOT EXISTS `parent_id` bigint NULL DEFAULT NULL COMMENT '父块编号(ParentChild 子块用)' AFTER `vector_key`;
```

- [ ] **Step 2: ChunkMapper**

`dal/mysql/ChunkMapper.java`:

```java
package cn.iocoder.yudao.module.ingestion.dal.mysql;

import cn.iocoder.yudao.framework.mybatis.core.mapper.BaseMapperX;
import cn.iocoder.yudao.module.ingestion.dal.dataobject.ChunkDO;
import org.apache.ibatis.annotations.Mapper;

@Mapper
public interface ChunkMapper extends BaseMapperX<ChunkDO> {
}
```

- [ ] **Step 3: ChunkStore 接口(三写编排)**

`store/ChunkStore.java`:

```java
package cn.iocoder.yudao.module.ingestion.store;

import cn.iocoder.yudao.module.ingestion.dal.dataobject.ChunkDO;

import java.util.List;

/**
 * 知识片段三写存储(MySQL + Milvus + ES)
 */
public interface ChunkStore {

    /**
     * 批量写入
     *
     * @param chunks 已含 vector 的片段
     * @param tenantId 租户编号
     * @param kbId 知识库编号
     */
    void saveChunks(List<ChunkDO> chunks, Long tenantId, Long kbId);

}
```

- [ ] **Step 4: MysqlChunkStore**

`store/MysqlChunkStore.java`:

```java
package cn.iocoder.yudao.module.ingestion.store;

import cn.iocoder.yudao.module.ingestion.dal.dataobject.ChunkDO;
import cn.iocoder.yudao.module.ingestion.dal.mysql.ChunkMapper;
import jakarta.annotation.Resource;
import org.springframework.stereotype.Component;

import java.util.List;

/**
 * MySQL 写入(ai_chunk)
 */
@Component
public class MysqlChunkStore {

    @Resource
    private ChunkMapper chunkMapper;

    public void insertChunks(List<ChunkDO> chunks) {
        for (ChunkDO chunk : chunks) {
            chunkMapper.insert(chunk);
        }
    }

}
```

- [ ] **Step 5: MilvusChunkStore**

`store/MilvusChunkStore.java`:

```java
package cn.iocoder.yudao.module.ingestion.store;

import io.milvus.client.MilvusServiceClient;
import io.milvus.common.clientenum.ConsistencyLevelEnum;
import io.milvus.param.ConnectParam;
import io.milvus.param.IndexType;
import io.milvus.param.MetricType;
import io.milvus.param.R;
import io.milvus.param.collection.CreateCollectionParam;
import io.milvus.param.collection.FieldType;
import io.milvus.param.collection.HasCollectionParam;
import io.milvus.param.collection.LoadCollectionParam;
import io.milvus.param.dml.InsertParam;
import io.milvus.param.index.CreateIndexParam;
import jakarta.annotation.PostConstruct;
import jakarta.annotation.Resource;
import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.util.ArrayList;
import java.util.List;

/**
 * Milvus 向量写入
 */
@Slf4j
@Component
public class MilvusChunkStore {

    @Value("${yudao.ai.milvus.host:127.0.0.1}")
    private String host;
    @Value("${yudao.ai.milvus.port:19530}")
    private Integer port;
    @Value("${yudao.ai.milvus.collection:ai_chunk_vector}")
    private String collection;
    @Value("${yudao.ai.milvus.dim:1024}")
    private Integer dim;

    private MilvusServiceClient client;

    @PostConstruct
    public void init() {
        client = new MilvusServiceClient(ConnectParam.newBuilder()
                .withHost(host)
                .withPort(port)
                .build());
        createCollectionIfAbsent();
    }

    private void createCollectionIfAbsent() {
        R<Boolean> has = client.hasCollection(HasCollectionParam.newBuilder()
                .withCollectionName(collection).build());
        if (has.getData() != null && has.getData()) {
            return;
        }
        FieldType chunkId = FieldType.newBuilder()
                .withName("chunk_id").withDataType(io.milvus.common.clientenum.DataType.Int64).withPrimaryKey(true).build();
        FieldType embedding = FieldType.newBuilder()
                .withName("embedding").withDataType(io.milvus.common.clientenum.DataType.FloatVector).withDimension(dim).build();
        FieldType tenantId = FieldType.newBuilder()
                .withName("tenant_id").withDataType(io.milvus.common.clientenum.DataType.Int64).build();
        FieldType kbId = FieldType.newBuilder()
                .withName("kb_id").withDataType(io.milvus.common.clientenum.DataType.Int64).build();
        CreateCollectionParam param = CreateCollectionParam.newBuilder()
                .withCollectionName(collection)
                .withFieldTypes(List.of(chunkId, embedding, tenantId, kbId))
                .withConsistencyLevel(ConsistencyLevelEnum.STRONG)
                .build();
        client.createCollection(param);
        // 建索引
        client.createIndex(CreateIndexParam.newBuilder()
                .withCollectionName(collection)
                .withFieldName("embedding")
                .withIndexType(IndexType.IVF_FLAT)
                .withMetricType(MetricType.COSINE)
                .withExtraParam("{\"nlist\":1024}")
                .build());
        client.loadCollection(LoadCollectionParam.newBuilder()
                .withCollectionName(collection).build());
        log.info("[init][Milvus 集合 {} 创建完成]", collection);
    }

    /**
     * 批量插入向量
     *
     * @param chunkIds 片段 id(与 MySQL 对应)
     * @param vectors 向量列表
     * @param tenantId 租户
     * @param kbId 知识库
     */
    public void insertVectors(List<Long> chunkIds, List<List<Float>> vectors, Long tenantId, Long kbId) {
        List<InsertParam.Field> fields = new ArrayList<>();
        fields.add(new InsertParam.Field("chunk_id", chunkIds));
        fields.add(new InsertParam.Field("embedding", vectors));
        fields.add(new InsertParam.Field("tenant_id", chunkIds.stream().map(x -> tenantId).toList()));
        fields.add(new InsertParam.Field("kb_id", chunkIds.stream().map(x -> kbId).toList()));
        InsertParam param = InsertParam.newBuilder()
                .withCollectionName(collection)
                .withFields(fields)
                .build();
        client.insert(param);
    }

}
```

- [ ] **Step 6: EsChunkStore**

`store/EsChunkStore.java`:

```java
package cn.iocoder.yudao.module.ingestion.store;

import com.fasterxml.jackson.databind.ObjectMapper;
import jakarta.annotation.PostConstruct;
import lombok.extern.slf4j.Slf4j;
import org.apache.http.HttpHost;
import org.elasticsearch.client.Request;
import org.elasticsearch.client.RequestOptions;
import org.elasticsearch.client.RestClient;
import org.elasticsearch.client.RestHighLevelClient;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.util.HashMap;
import java.util.Map;

/**
 * ES BM25 写入
 */
@Slf4j
@Component
public class EsChunkStore {

    @Value("${spring.elasticsearch.uris:http://127.0.0.1:9200}")
    private String uris;
    @Value("${yudao.ai.es.index:ai_chunk_bm25}")
    private String index;

    private RestClient client;
    private final ObjectMapper objectMapper = new ObjectMapper();

    @PostConstruct
    public void init() {
        String host = uris.replaceAll("^https?://", "").split(":")[0];
        int port = Integer.parseInt(uris.replaceAll("^https?://", "").split(":")[1].replaceAll("/.*", ""));
        client = RestClient.builder(new HttpHost(host, port, "http")).build();
        createIndexIfAbsent();
    }

    private void createIndexIfAbsent() {
        try {
            Request exists = new Request("HEAD", "/" + index);
            int code = client.performRequest(exists).getStatusLine().getStatusCode();
            if (code == 200) {
                return;
            }
            Map<String, Object> mapping = new HashMap<>();
            mapping.put("mappings", Map.of("properties", Map.of(
                    "chunk_id", Map.of("type", "keyword"),
                    "tenant_id", Map.of("type", "keyword"),
                    "kb_id", Map.of("type", "keyword"),
                    "status", Map.of("type", "keyword"),
                    "content", Map.of("type", "text",
                            "analyzer", "ik_max_word", "search_analyzer", "ik_smart")
            )));
            Request create = new Request("PUT", "/" + index);
            create.setJsonEntity(objectMapper.writeValueAsString(mapping));
            client.performRequest(create);
            log.info("[init][ES 索引 {} 创建完成]", index);
        } catch (Exception e) {
            log.error("[init][ES 索引创建失败]", e);
        }
    }

    /**
     * 写入一条 BM25 文档
     */
    public void insertChunk(Long chunkId, Long tenantId, Long kbId, String content) {
        try {
            Map<String, Object> doc = new HashMap<>();
            doc.put("chunk_id", String.valueOf(chunkId));
            doc.put("tenant_id", String.valueOf(tenantId));
            doc.put("kb_id", String.valueOf(kbId));
            doc.put("status", "PUBLISHED");
            doc.put("content", content);
            Request request = new Request("PUT", "/" + index + "/_doc/" + chunkId);
            request.setJsonEntity(objectMapper.writeValueAsString(doc));
            client.performRequest(request);
        } catch (Exception e) {
            throw new RuntimeException("ES 写入失败: " + e.getMessage(), e);
        }
    }

}
```

> 注意: 若 ik 分词插件未装(8-中间件清单说明 ik 可选), analyzer 会报错。开发环境先去掉 analyzer 配置或用 standard 兜底; 若 ES 启动时未装 ik, 将 mapping 中 content 改为:

```java
"content", Map.of("type", "text")
```

- [ ] **Step 7: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-ingestion/yudao-module-ingestion-server -am compile -q -DskipTests
```

> 若 milvus-sdk / es-rest-client 有 API 差异(版本 2.4.1 / 8.13.4),按编译错误调整方法签名。es-rest-client 8.x 的 `RestHighLevelClient` 已废弃,若编译失败改用 `RestClient` + low-level JSON。

Expected: BUILD SUCCESS

- [ ] **Step 8: 提交**

```bash
git add yudao-module-ingestion
git commit -m "feat(ingestion): MySQL/Milvus/ES 三写存储组件"
```

---

### Task 9: ingestion-server — 主流程服务(解析→切分→向量化→三写)

**Files:**
- Create: `.../ingestion/embedding/EmbeddingClient.java`
- Create: `.../ingestion/service/IngestService.java`
- Create: `.../ingestion/service/IngestServiceImpl.java`
- Create: `.../ingestion/mq/KnowledgeIngestConsumer.java`
- Modify: `.../ingestion/api/IngestionApiImpl.java`(可选, 暴露触发接口)

- [ ] **Step 1: EmbeddingClient(Feign 调 model-server)**

`embedding/EmbeddingClient.java`:

```java
package cn.iocoder.yudao.module.ingestion.embedding;

import cn.iocoder.yudao.module.model.api.ModelApi;
import jakarta.annotation.Resource;
import org.springframework.stereotype.Component;

import java.util.List;

/**
 * 向量化客户端: Feign 调 model-server 的 embedding 接口
 */
@Component
public class EmbeddingClient {

    @Resource
    private ModelApi modelApi;

    /**
     * 批量向量化
     *
     * @param texts 文本列表
     * @return 每个文本的 1024 维向量
     */
    public List<List<Float>> embed(List<String> texts) {
        if (texts == null || texts.isEmpty()) {
            return List.of();
        }
        return modelApi.embedding(texts);
    }

}
```

> 确认 ingestion-server 已引入 `yudao-module-model-api` 依赖(Task 5 已加)。若 Feign 扫描不生效,在 `RpcConfiguration.java`(已有)中确认 `@EnableFeignClients` 扫描 `cn.iocoder.yudao.module.model.api`。

- [ ] **Step 2: IngestService 接口**

`service/IngestService.java`:

```java
package cn.iocoder.yudao.module.ingestion.service;

/**
 * 文档入库服务
 */
public interface IngestService {

    /**
     * 处理单个文档入库
     *
     * @param documentId 文档编号
     */
    void ingestDocument(Long documentId);

}
```

- [ ] **Step 3: IngestServiceImpl(主流程)**

`service/IngestServiceImpl.java`:

```java
package cn.iocoder.yudao.module.ingestion.service;

import cn.hutool.core.util.StrUtil;
import cn.iocoder.yudao.framework.common.exception.util.ServiceExceptionUtil;
import cn.iocoder.yudao.module.ingestion.dal.dataobject.ChunkDO;
import cn.iocoder.yudao.module.ingestion.embedding.EmbeddingClient;
import cn.iocoder.yudao.module.ingestion.parse.DocumentParser;
import cn.iocoder.yudao.module.ingestion.parse.OfficeParser;
import cn.iocoder.yudao.module.ingestion.parse.PdfParser;
import cn.iocoder.yudao.module.ingestion.parse.TextParser;
import cn.iocoder.yudao.module.ingestion.split.Chunk;
import cn.iocoder.yudao.module.ingestion.split.SplitterFactory;
import cn.iocoder.yudao.module.ingestion.store.ChunkStore;
import cn.iocoder.yudao.module.ingestion.store.MysqlChunkStore;
import cn.iocoder.yudao.module.ingestion.store.MilvusChunkStore;
import cn.iocoder.yudao.module.ingestion.store.EsChunkStore;
import cn.iocoder.yudao.module.knowledge.api.KnowledgeApi;
import jakarta.annotation.Resource;
import lombok.extern.slf4j.Slf4j;
import org.springframework.stereotype.Service;

import java.util.ArrayList;
import java.util.List;

/**
 * 文档入库主流程: 解析 → 切分 → 向量化 → 三写
 */
@Slf4j
@Service
public class IngestServiceImpl implements IngestService {

    @Resource
    private TextParser textParser;
    @Resource
    private PdfParser pdfParser;
    @Resource
    private OfficeParser officeParser;
    @Resource
    private SplitterFactory splitterFactory;
    @Resource
    private EmbeddingClient embeddingClient;
    @Resource
    private MysqlChunkStore mysqlChunkStore;
    @Resource
    private MilvusChunkStore milvusChunkStore;
    @Resource
    private EsChunkStore esChunkStore;
    @Resource
    private KnowledgeApi knowledgeApi;

    @Override
    public void ingestDocument(Long documentId) {
        // 1. 置为解析中
        knowledgeApi.updateDocumentParseStatus(documentId, "PARSING", null);
        try {
            // 2. 查询文档信息(knowledge-server 提供, 暂通过知识库接口补齐; 若没有文档查询接口, 先以 documentId 关联)
            // TODO: 需要 knowledge-server 提供文档详情查询 Feign(名称/类型/存储路径/知识库/租户/切分策略)
            //   本步骤先聚焦主链路, 用一个默认策略 + 占位元数据跑通, 下一迭代补详情查询
            String docType = "TXT";      // 占位: 需从文档详情取
            String storagePath = "";     // 占位: 需从文档详情取(MinIO URL)
            Long kbId = 1L;              // 占位: 需从文档详情取
            Long tenantId = 1L;          // 占位: 需从文档详情取
            String chunkStrategy = "ParentChild"; // 占位: 需从知识库配置取

            // 3. 解析
            String filePath = downloadFromMinio(storagePath);
            DocumentParser parser = chooseParser(docType);
            String text = parser.parse(filePath, docType);

            // 4. 切分
            List<Chunk> chunks = splitterFactory.getSplitter(chunkStrategy)
                    .split(text, 500);

            // 5. 向量化(批量)
            List<String> contents = chunks.stream().map(Chunk::getContent).toList();
            List<List<Float>> vectors = embeddingClient.embed(contents);

            // 6. 三写: 先 MySQL, 再 Milvus, 再 ES
            List<ChunkDO> dos = new ArrayList<>();
            List<Long> chunkIds = new ArrayList<>();
            List<List<Float>> validVectors = new ArrayList<>();
            for (int i = 0; i < chunks.size(); i++) {
                ChunkDO chunkDO = new ChunkDO();
                chunkDO.setVersionId(documentId); // 版本状态机后续接入, 暂用文档 id
                chunkDO.setContent(chunks.get(i).getContent());
                chunkDO.setChunkType(chunks.get(i).getChunkType());
                chunkDO.setStatus("PUBLISHED");
                chunkDO.setMetadata(chunks.get(i).getMetadata());
                chunkDO.setParentId(chunks.get(i).getParentId());
                mysqlChunkStore.insertChunks(List.of(chunkDO));
                chunkDO.setVectorKey(String.valueOf(chunkDO.getId()));
                chunkIds.add(chunkDO.getId());
                validVectors.add(vectors.get(i));
                // ES 写入
                esChunkStore.insertChunk(chunkDO.getId(), tenantId, kbId, chunkDO.getContent());
            }
            // Milvus 批量写
            milvusChunkStore.insertVectors(chunkIds, validVectors, tenantId, kbId);

            // 7. 置为已入库
            knowledgeApi.updateDocumentParseStatus(documentId, "INDEXED", null);
            log.info("[ingestDocument][文档 {} 入库完成, {} 个片段]", documentId, chunks.size());
        } catch (Exception e) {
            log.error("[ingestDocument][文档 {} 入库失败]", documentId, e);
            knowledgeApi.updateDocumentParseStatus(documentId, "FAILED", StrUtil.sub(e.getMessage(), 0, 500));
        }
    }

    private DocumentParser chooseParser(String docType) {
        return switch (docType) {
            case "PDF" -> pdfParser;
            case "WORD", "EXCEL", "PPT" -> officeParser;
            default -> textParser; // TXT / MD
        };
    }

    private String downloadFromMinio(String storagePath) {
        // TODO: 从 MinIO 下载(storagePath 为 URL); 第一步以本地文件跑通, 后续接 infra FileApi
        throw new UnsupportedOperationException("MinIO 下载待接入, 第一步用本地文件验证");
    }

}
```

> ⚠️ 主流程中"查询文档详情 / MinIO 下载"存在占位。为满足"第一步可跑通", Task 10 补 knowledge-server 的文档详情 Feign 接口与 MinIO 下载,并替换上述占位。若先跑通本地文件: 将 `downloadFromMinio` 改为读本地路径 `filePath = storagePath`(开发环境直接把文件放 ingestion-server 的 /tmp 下, 由 Task 10 的验证脚本指定)。

- [ ] **Step 4: Kafka 消费者**

`mq/KnowledgeIngestConsumer.java`:

```java
package cn.iocoder.yudao.module.ingestion.mq;

import cn.iocoder.yudao.module.ingestion.service.IngestService;
import jakarta.annotation.Resource;
import lombok.extern.slf4j.Slf4j;
import org.springframework.kafka.annotation.KafkaListener;
import org.springframework.stereotype.Component;

/**
 * 文档入库消息消费者
 */
@Slf4j
@Component
public class KnowledgeIngestConsumer {

    @Resource
    private IngestService ingestService;

    @KafkaListener(topics = "knowledge-ingest", groupId = "ingestion-server")
    public void onMessage(Long documentId) {
        log.info("[onMessage][收到入库任务, documentId={}]", documentId);
        ingestService.ingestDocument(documentId);
    }

}
```

> 若反序列化 Long 失败,生产者发送 String(documentId) 时,消费者参数改 `String documentId` 并 `Long.parseLong(documentId)`。

- [ ] **Step 5: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-ingestion/yudao-module-ingestion-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 6: 提交**

```bash
git add yudao-module-ingestion
git commit -m "feat(ingestion): 入库主流程(Kafka 消费→解析→切分→向量化→三写)"
```

---

### Task 10: knowledge-server — 文档详情查询 Feign(供 ingestion 取元数据)

**Files:**
- Modify: `yudao-module-knowledge/yudao-module-knowledge-api/.../api/KnowledgeApi.java`
- Modify: `yudao-module-knowledge/yudao-module-knowledge-server/.../api/KnowledgeApiImpl.java`
- Create: `yudao-module-knowledge/yudao-module-knowledge-api/.../api/dto/KnowledgeDocumentRespDTO.java`

- [ ] **Step 1: DTO**

`KnowledgeDocumentRespDTO.java`:

```java
package cn.iocoder.yudao.module.knowledge.api.dto;

import lombok.Data;

@Data
public class KnowledgeDocumentRespDTO {

    /** 文档编号 */
    private Long id;

    /** 知识库编号 */
    private Long kbId;

    /** 文档名 */
    private String name;

    /** 类型: TXT/MD/PDF/WORD/EXCEL/PPT */
    private String type;

    /** 存储路径(MinIO URL) */
    private String storagePath;

    /** 解析状态 */
    private String parseStatus;

    /** 租户编号 */
    private Long tenantId;

}
```

- [ ] **Step 2: KnowledgeApi 加查询方法**

```java
    /**
     * 查询文档详情(供 ingestion-server 取元数据)
     */
    @GetMapping("/get-document")
    KnowledgeDocumentRespDTO getDocument(@RequestParam("id") Long id);
```

- [ ] **Step 3: 实现**

`KnowledgeApiImpl.java` 增加:

```java
    @Override
    public KnowledgeDocumentRespDTO getDocument(Long id) {
        AiDocumentDO doc = aiDocumentService.getAiDocument(id);
        if (doc == null) {
            return null;
        }
        KnowledgeDocumentRespDTO dto = new KnowledgeDocumentRespDTO();
        dto.setId(doc.getId());
        dto.setKbId(doc.getKbId());
        dto.setName(doc.getName());
        dto.setType(doc.getType());
        dto.setStoragePath(doc.getStoragePath());
        dto.setParseStatus(doc.getParseStatus());
        dto.setTenantId(doc.getTenantId());
        return dto;
    }
```

> `AiDocumentDO` 若没有 tenantId 字段,从 `BaseDO` 获取或跳过;芋道 BaseDO 有 `tenantId`。检查 `AiDocumentDO` 继承关系。

- [ ] **Step 4: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-knowledge/yudao-module-knowledge-api,yudao-module-knowledge/yudao-module-knowledge-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 5: 提交**

```bash
git add yudao-module-knowledge
git commit -m "feat(knowledge): 文档详情查询 Feign 接口(供入库管线取元数据)"
```

---

### Task 11: ingestion-server — 接入文档详情 + MinIO 下载,替换占位

**Files:**
- Modify: `.../ingestion/service/IngestServiceImpl.java`
- Modify: `.../ingestion/embedding/EmbeddingClient.java`(无改动)
- Modify: `yudao-module-ingestion/yudao-module-ingestion-server/pom.xml`(+ infra-api 依赖, 用于文件下载)

- [ ] **Step 1: pom 加 infra-api(MinIO 文件下载)**

```xml
        <dependency>
            <groupId>cn.iocoder.cloud</groupId>
            <artifactId>yudao-module-infra-api</artifactId>
        </dependency>
```

- [ ] **Step 2: 替换 IngestServiceImpl 的占位**

将 `ingestDocument` 方法中"2. 查询文档信息"与"3. 解析"的占位替换为:

```java
            // 2. 查询文档信息
            KnowledgeDocumentRespDTO document = knowledgeApi.getDocument(documentId);
            if (document == null) {
                throw new RuntimeException("文档不存在: " + documentId);
            }
            String docType = document.getType();
            String storagePath = document.getStoragePath();
            Long kbId = document.getKbId();
            Long tenantId = document.getTenantId() != null ? document.getTenantId() : 1L;
            String chunkStrategy = getKnowledgeBaseStrategy(kbId); // 见 Step 3

            // 3. 解析(从 MinIO 下载到本地临时文件)
            String filePath = downloadFromMinio(storagePath);
            DocumentParser parser = chooseParser(docType);
            String text = parser.parse(filePath, docType);
```

- [ ] **Step 3: 知识库切分策略查询 + MinIO 下载**

新增私有方法(使用 infra 的 FileApi 或直接 HTTP 下载;开发环境 MinIO 无鉴权,直接用 URL 下载):

```java
    private String getKnowledgeBaseStrategy(Long kbId) {
        // 先返回默认, 知识库详情 Feign 后续接入
        return "ParentChild";
    }

    private String downloadFromMinio(String storagePath) {
        if (StrUtil.isBlank(storagePath)) {
            throw new RuntimeException("存储路径为空");
        }
        // MinIO URL 形如 http://127.0.0.1:9000/kb-docs/xxx
        String fileName = StrUtil.subAfter(storagePath, "/", true);
        String tmpFile = System.getProperty("java.io.tmpdir") + "/" + fileName;
        cn.hutool.http.HttpUtil.downloadFile(storagePath, tmpFile);
        return tmpFile;
    }
```

> `cn.hutool.http.HttpUtil` 由芋道 common 依赖(hutool)提供,无需额外引入。若 MinIO URL 需要鉴权(本环境未开),暂可直接下载。

- [ ] **Step 4: 编译验证**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
mvn -pl yudao-module-ingestion/yudao-module-ingestion-server -am compile -q -DskipTests
```

Expected: BUILD SUCCESS

- [ ] **Step 5: 提交**

```bash
git add yudao-module-ingestion
git commit -m "feat(ingestion): 接入文档详情与 MinIO 下载, 移除占位"
```

---

### Task 12: 前端 — 切分策略 5 种 + 失败原因展示

**Files:**
- Modify: `/Users/wudi/IdealProjects/myself/yudao-ui-admin-vben-master/apps/web-antd/src/views/ai/knowledge/data.ts`
- Modify: `/Users/wudi/IdealProjects/myself/yudao-ui-admin-vben-master/apps/web-antd/src/views/ai/document/index.vue`

- [ ] **Step 1: 切分策略选项改为 5 种**

在 `views/ai/knowledge/data.ts` 的 `chunkStrategy` 字段 options 改为:

```ts
        options: [
          { label: '语义切分 (Semantic)', value: 'Semantic' },
          { label: '父子切分 (ParentChild)', value: 'ParentChild' },
          { label: '表格切分 (Table)', value: 'Table' },
          { label: '问答切分 (FAQ)', value: 'FAQ' },
          { label: '条款切分 (Policy)', value: 'Policy' },
        ],
```

- [ ] **Step 2: 文档页失败原因展示**

在 `views/ai/document/index.vue` 的 `STATUS_TAG` 中 FAILED 增加 `errorMsg` 展示(在 status 模板加 Tooltip):

```vue
      <template #status="{ row }">
        <Tag :color="(row.parseStatus && STATUS_TAG[row.parseStatus]?.color) || 'default'">
          {{ (row.parseStatus && STATUS_TAG[row.parseStatus]?.text) || row.parseStatus }}
        </Tag>
        <a-tooltip v-if="row.parseStatus === 'FAILED' && row.errorMsg" :title="row.errorMsg">
          <span class="ml-1 text-xs text-red-500">ⓘ</span>
        </a-tooltip>
      </template>
```

> `a-tooltip` 来自 ant-design-vue,页面已全局注册,无需额外 import。`row.errorMsg` 需在 `data.ts` 的列定义或类型 `KnowledgeDocument` 中补充(API 返回 error_msg 已由 Task 1 DDL 支持)。

- [ ] **Step 3: 前端编译验证(dev server 已运行,自动 HMR)**

```bash
curl -s --noproxy '*' -o /dev/null -w "%{http_code}" "http://127.0.0.1:5666/src/views/ai/knowledge/data.ts"
curl -s --noproxy '*' -o /dev/null -w "%{http_code}" "http://127.0.0.1:5666/src/views/ai/document/index.vue"
```

Expected: 200 / 200

- [ ] **Step 4: 提交(前端仓库)**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-ui-admin-vben-master
git add apps/web-antd/src/views/ai/knowledge/data.ts apps/web-antd/src/views/ai/document/index.vue
git commit -m "feat(ai): 切分策略改 5 种, 文档失败原因展示"
```

---

### Task 13: 数据库 DDL 增量执行

**Files:**
- Execute: MySQL(库 ruoyi-vue-pro)

- [ ] **Step 1: 执行 ai_chunk.parent_id 与 ai_document.error_msg 增量 DDL**

```bash
docker exec yudao-mysql mysql -uroot -p123456 ruoyi-vue-pro -e "
ALTER TABLE \`ai_chunk\` ADD COLUMN IF NOT EXISTS \`parent_id\` bigint NULL DEFAULT NULL COMMENT '父块编号(ParentChild 子块用)' AFTER \`vector_key\`;
ALTER TABLE \`ai_document\` ADD COLUMN IF NOT EXISTS \`error_msg\` varchar(512) NULL DEFAULT NULL COMMENT '失败原因' AFTER \`parse_status\`;
"
```

Expected: Query OK, 0 rows affected(或提示已存在)

- [ ] **Step 2: 验证列已存在**

```bash
docker exec yudao-mysql mysql -uroot -p123456 ruoyi-vue-pro -e "SHOW COLUMNS FROM ai_chunk LIKE 'parent_id'; SHOW COLUMNS FROM ai_document LIKE 'error_msg';"
```

Expected: 各返回一行

---

### Task 14: 端到端联调验证

**Files:**
- 无(验证操作)

- [ ] **Step 1: 重启/启动全部相关服务**

```bash
cd /Users/wudi/IdealProjects/myself/yudao-cloud-mini-master-jdk17
# IDEA 启动(或命令行):
#   gateway(48080) / system(48081) / infra(48082) 已在跑
#   knowledge-server(48084) — 需重启(代码变更)
#   model-server(48091) — 需重启(代码变更)
#   ingestion-server(48085) — 首次启动
```

> 若用命令行验证,先 `mvn -pl yudao-module-knowledge/yudao-module-knowledge-server -am package -DskipTests` 等构建,再用 `java -jar` 启动;日常建议 IDEA 启动。

- [ ] **Step 2: 健康检查**

```bash
for p in 48080 48081 48082 48084 48085 48091; do
  curl -s --noproxy '*' -o /dev/null -w "$p:%{http_code} " http://127.0.0.1:$p/actuator/health
done; echo
```

Expected: 全部 200

- [ ] **Step 3: 登录拿 token**

```bash
TOKEN=$(curl -s --noproxy '*' -X POST http://127.0.0.1:48080/admin-api/system/auth/login \
  -H "Content-Type: application/json" -H "tenant-id: 1" \
  -d '{"username":"admin","password":"admin123"}' | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['accessToken'])")
echo "token=${#TOKEN} 字符"
```

- [ ] **Step 4: 上传测试文档(TXT)**

```bash
echo "碎屏保修政策: 购机 730 天内出现屏幕质量问题, 可免费维修。保修需提供购机发票。" > /tmp/test-kb.md
curl -s --noproxy '*' -X POST http://127.0.0.1:48080/admin-api/infra/file/upload \
  -H "Authorization: Bearer $TOKEN" -H "tenant-id: 1" \
  -F "file=@/tmp/test-kb.md" -F "directory=kb-docs"
```

Expected: 返回 URL

- [ ] **Step 5: 登记文档(触发 Kafka)**

```bash
URL=<上一步返回的 url>
curl -s --noproxy '*' -X POST http://127.0.0.1:48080/admin-api/knowledge/document/create \
  -H "Authorization: Bearer $TOKEN" -H "tenant-id: 1" -H "Content-Type: application/json" \
  -d "{\"kbId\":1,\"name\":\"test-kb.md\",\"type\":\"MD\",\"storagePath\":\"$URL\"}"
```

Expected: 返回 id, parse_status 应为 PENDING → 随后 ingestion 消费变 PARSING → INDEXED

- [ ] **Step 6: 验证状态流转**

```bash
# 轮询文档状态(等 5~10 秒)
curl -s --noproxy '*' "http://127.0.0.1:48080/admin-api/knowledge/document/page?pageNo=1&pageSize=10" \
  -H "Authorization: Bearer $TOKEN" -H "tenant-id: 1" | python3 -c "import sys,json; [print(x['id'], x['name'], x['parseStatus']) for x in json.load(sys.stdin)['data']['list']]"
```

Expected: parseStatus = INDEXED

- [ ] **Step 7: 验证三写数据**

```bash
# MySQL ai_chunk
docker exec yudao-mysql mysql -uroot -p123456 ruoyi-vue-pro -e "SELECT COUNT(*) AS chunk_cnt FROM ai_chunk WHERE deleted=0;"
# Milvus 集合行数
docker exec yudao-milvus sh -c "echo 'not trivial via cli'"; # 或通过 milvus_cli / SDK 验证
# ES 索引行数
curl -s --noproxy '*' "http://127.0.0.1:9200/ai_chunk_bm25/_count" | python3 -c "import sys,json; print('ES count:', json.load(sys.stdin).get('count'))"
```

Expected: MySQL chunk_cnt ≥ 1; ES count ≥ 1(与 MySQL 行数一致)

- [ ] **Step 8: 失败用例验证(上传损坏文件)**

```bash
echo "not a real pdf" > /tmp/bad.pdf
# 上传 → 登记 → 观察 parseStatus = FAILED 且 error_msg 非空
```

Expected: parseStatus = FAILED, error_msg 显示解析失败原因

- [ ] **Step 9: 前端验证**

浏览器打开 `http://127.0.0.1:5666`,进入 `kb/documents`:
- 知识库下拉可选(测试知识库)
- 上传 MD 文件后状态从"待解析"流转到"已入库"
- `kb/knowledge-base` 新建知识库时切分策略显示 5 种

---

## 自检记录

- **Spec 覆盖**:五策略(Task 7)✓;全格式解析(Task 6)✓;OCR(Task 6 PdfParser 说明 + Tika)✓;BGE-M3 向量化(Task 4 + Task 9)✓;三写(Task 8)✓;Kafka 触发(Task 2/9)✓;状态回写(Task 3/9)✓;前端 5 策略+失败展示(Task 12)✓;幂等/重试(设计 5.4,Task 9 catch → FAILED,消费幂等待优化标记)
- **占位扫描**:Task 9 中"文档详情查询/切分策略查询"占位已在 Task 10/11 补齐;`getKnowledgeBaseStrategy` 仍返回默认值,已在计划中注明后续接知识库 Feign
- **类型一致性**:`Chunk`(ingestion.split)→ `ChunkDO`(ingestion.dal)→ `ai_chunk` 表;`KnowledgeDocumentRespDTO` 字段与 `AiDocumentDO` 对齐;`ModelApi.embedding` 返回 `List<List<Float>>` 与 Milvus 向量类型一致
