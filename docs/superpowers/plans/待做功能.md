1、系统当中用到的模型   如对话、重排、向量  配置文件全不替换成数据库里面配置的模型，加载的时候也要按照需求加载对应的模型配置信息。
2、没有用到的模型去掉，目前用到的yudao:
model:
embedding:
base-url: http://127.0.0.1:1234/v1   # LM Studio
model: text-embedding-bge-m3
chat:
base-url: http://127.0.0.1:1234/v1   # LM Studio
model: qwen/qwen3-8b                  # 已确认加载; 备选 qwen/qwen3-14b
rerank:
base-url: http://127.0.0.1:1236/v1   # llama.cpp server(bge-reranker-v2-m3 Q8_0, 见 deploy/llama-rerank.sh)
model: bge-reranker-v2-m3

不需要再配置文件中在单独配置

3、知识库列表 这个功能里面的切分策略  要去掉，切分策略应该是基于文档的，不是针对知识库列表

4、入库管线 可以选择切分策略，现在的切分策略是固定的几个感觉有问题，而且现在使用的都是固定的语义切分，没有图片识别功能，比如我要切专利的，肯定会有图片，还要考虑上下文的连贯性等等，这些问题都要考虑到，
是不是可以把切分策略可以作为一个插件形式适配到项目中，后面可以定制化开发切分策略，来应对不同的场景
5、现在意图管理、槽位管理 是为了提高搜索准确率吗？现在这两个功能我觉得很不好用，如果去掉对准确率有影响吗？
