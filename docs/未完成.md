可以。我按你现在这个项目的实际阶段，给你整理成一份**从当前状态一路做到真正 Enterprise Ready 的后续总清单**。后面就按这个顺序推进，不要再看到一个 Case 就临时往里加功能。

先提醒一个小点：我刚从 `refactor/knowledge-platform-business-p0` 读取到的远端代码里，`ChatController` 目前仍有 `finally { sink.complete(); }`，`SseChatStreamSink.complete()` 也还是直接调用 `emitter.complete()`，所以我这里把 **SSE Lifecycle Final Verify** 放在第 0 项。如果你刚提交的新 commit 还没同步到连接器，等它可见后再复查一次即可。

# AI-Knowledge 后续总执行清单

## Phase 0：当前 P0 主链正式收口

这是现在立刻做的，**这一阶段没完成之前不要开 Agent、知识图谱、Copilot、Prior Art。**

### P0-00 SSE Lifecycle Final Verify

* [ ] `OPEN / COMPLETED / CLIENT_CANCELLED / TIMEOUT / ERROR` 状态机
* [ ] terminal state CAS，只允许成功一次
* [ ] 客户端取消后禁止再次 `emitter.complete()`
* [ ] `send after cancel` 直接 no-op
* [ ] Transport IOException 正确识别为断开
* [ ] 点击“停止生成”不报系统错误
* [ ] 一个用户不能同时启动两条生成链
* [ ] 旧 AbortController 不得误杀新请求
* [ ] 连续 20 次流式请求无 Tomcat NPE
* [ ] AccessLog 异步 500 与 Chat 主链隔离
* [ ] SSE 真正耗时由 Query Trace 记录，不使用 MVC `afterCompletion=0ms`
* [ ] 单元测试 + 真实 API 回归

**Gate：**

```text
正常完成 → DONE
手工停止 → CLIENT_CANCELLED
超时 → TIMEOUT
异常 → ERROR

四种状态均不能 double-complete
```

---

## P0-01 通用 Structured Query Engine

这就是刚刚我们定的通用能力。

* [ ] `StructuredQueryPlan`
* [ ] `QueryScope`
* [ ] `MetricDefinition`
* [ ] `DomainMetricRegistry`
* [ ] `DomainEntityRegistry`
* [ ] `DomainStructuredDataAdapter`
* [ ] `StructuredQueryExecutor`
* [ ] `ContextResolver`
* [ ] `ScopeResolver`
* [ ] `CompletenessGuard`
* [ ] `CLARIFY`
* [ ] `COUNT`
* [ ] `COUNT_DISTINCT`
* [ ] `SUM`
* [ ] `AVG`
* [ ] `MIN`
* [ ] `MAX`
* [ ] `LIST`
* [ ] `GROUP`
* [ ] `TOP_N`
* [ ] Core 不 import Patent package
* [ ] Fake 非 Patent Domain 测试通过

Patent 作为第一个 Domain Pack：

* [ ] `PATENT_DOCUMENT`
* [ ] `CLAIM`
* [ ] `DOCUMENT_COUNT`
* [ ] `CLAIM_COUNT`
* [ ] `INDEPENDENT_CLAIM_COUNT`
* [ ] `DEPENDENT_CLAIM_COUNT`
* [ ] aliases / synonyms
* [ ] Patent Structured Adapter

必须跑通：

```text
当前知识库有几个专利？
→ COUNT document

三个专利共有多少项权利要求？
→ SUM claimCount

平均多少？
→ AVG claimCount

哪个最多？
→ MAX + GROUP BY document

分别多少？
→ GROUP/LIST

最多的三个？
→ TOP_N
```

**目标：**

```text
明确 Structured Query：
0 Embedding
0 Vector
0 Rerank
0 Generate
0 Verify

P95 < 2s
```

---

## P0-02 Query Planner 最终统一

Planner 不能继续变成关键词路由器。

最终至少统一成：

```text
RULE

STRUCTURED_QUERY
├── EXACT_LOOKUP
├── AGGREGATE
├── LIST
├── GROUP
├── SORT
└── TOP_N

SEMANTIC_QUERY
├── SCOPED_RAG
└── HYBRID_RAG

CLARIFY
ABSTAIN
```

完成：

* [ ] Rule 只处理确定性规则
* [ ] PreParser 只负责 Candidate Detection
* [ ] 明确 Query 不调用 Planner LLM
* [ ] 有语义歧义时最多调用一次 Semantic Planner
* [ ] `Scope + Metric + Operation + GroupBy` 明确后才能执行
* [ ] 指代解析：它/这些/三个/上述/刚才那些
* [ ] 新实体覆盖旧实体
* [ ] 无法确定 Scope → CLARIFY
* [ ] 禁止 Structured Query 失败后偷偷 Hybrid
* [ ] 禁止 TopK 推导全集结论

---

## P0-03 Evidence / Citation 商用化

回答正确还不够，必须能证明。

* [ ] Evidence DTO 统一
* [ ] documentId
* [ ] documentName
* [ ] versionId/versionNo
* [ ] chunkId
* [ ] kbId
* [ ] domainCode
* [ ] sectionType
* [ ] sectionTitle
* [ ] page
* [ ] claimNo
* [ ] applicationNo/publicationNo
* [ ] content snapshot
* [ ] score
* [ ] evidenceType

支持：

```text
RETRIEVAL_EVIDENCE
STRUCTURED_RESULT
EXACT_METADATA
EXACT_CLAIM
```

Citation：

* [ ] `[C1]` 必须对应 Evidence 1
* [ ] Citation Validator
* [ ] Citation 越界禁止返回
* [ ] 引用内容必须和事实匹配
* [ ] Aggregate 不需要返回全部 Document/Chunk
* [ ] Structured Result 可独立作为 Evidence

---

## P0-04 历史回答完整持久化

目前聊天不能只存：

```text
role + content
```

需要：

* [ ] answer
* [ ] route
* [ ] intent
* [ ] confidence
* [ ] answerable
* [ ] degraded
* [ ] traceId
* [ ] citation
* [ ] Evidence snapshot
* [ ] document version snapshot
* [ ] structured result snapshot

要求：

```text
知识当前已经 V3

历史答案引用 V1

重新打开历史会话
→ 仍显示“当时依据 V1”
```

* [ ] F5 Evidence 不丢
* [ ] F5 Citation 不丢
* [ ] F5 Trace 不丢
* [ ] 文档升级后历史引用不漂移

---

## P0-05 Query Trace 最终版

每个问题：

```text
1 queryId
1 traceId
```

统一阶段：

```text
PIPELINE_ENTER
CONVERSATION_LOAD
PERMISSION_CHECK
CONTEXT_RESOLVE
ANALYZE
PLAN
SCOPE_RESOLVE
STRUCTURED_EXECUTE

BM25
VECTOR
FUSION
RERANK

EVIDENCE
GENERATE
VERIFY
REPAIR

MESSAGE_PERSIST
TRACE_PERSIST
PIPELINE_EXIT
```

完成：

* [ ] 每 Stage start/end/elapsed
* [ ] status
* [ ] input/output summary
* [ ] error code
* [ ] modelCallId
* [ ] SKIPPED 状态
* [ ] LLM 调用次数
* [ ] Vector 调用次数
* [ ] Token
* [ ] Cost
* [ ] totalMs
* [ ] `totalMs - accountedStages` 差值监控
* [ ] 不允许出现 17 秒 unexplained latency

---

## P0-06 Streaming Workbench 最终产品化

聊天页面完成：

* [ ] SSE delta
* [ ] Stop generating
* [ ] Thinking/执行过程区域
* [ ] 真实 Stage Event
* [ ] Evidence Drawer
* [ ] Citation 点击查看来源
* [ ] Trace 一键查看
* [ ] Knowledge Base 锁定
* [ ] 切 KB → 新会话
* [ ] History restore
* [ ] conversationId URL restore
* [ ] duplicate-send guard
* [ ] loading 正确
* [ ] error 正确
* [ ] cancel 正确
* [ ] dark mode 完整回归

注意：

“Thinking”只显示：

```text
正在理解问题
已定位知识范围
正在检索
找到 3 条证据
正在生成
校验通过
```

不展示模型内部原始 Chain-of-Thought。

---

## P0-07 Feedback 基础闭环

每条 AI Message：

```text
👍 有用
👎 无用
```

差评原因：

```text
回答错误
没回答到问题
证据不正确
信息不完整
知识过期
回答太慢
其它
```

存储：

* [ ] messageId
* [ ] queryId
* [ ] traceId
* [ ] conversationId
* [ ] userId
* [ ] tenantId
* [ ] kbId
* [ ] domainCode
* [ ] rating
* [ ] reason
* [ ] comment
* [ ] route
* [ ] model
* [ ] promptVersion
* [ ] evidence
* [ ] knowledgeVersion

P0 只：

> **收集。**

先不要自动训练模型。

---

## P0-08 Business Error + Deadline

清掉：

```text
500 系统异常
```

覆盖：

* [ ] KB_NOT_FOUND
* [ ] KB_FORBIDDEN
* [ ] DOMAIN_LOCKED
* [ ] CONVERSATION_NOT_FOUND
* [ ] CONVERSATION_FORBIDDEN
* [ ] CONVERSATION_CONTEXT_STALE
* [ ] DOCUMENT_NOT_FOUND
* [ ] DOCUMENT_NOT_PUBLISHED
* [ ] IDENTIFIER_NOT_FOUND
* [ ] STRUCTURED_SCOPE_AMBIGUOUS
* [ ] METRIC_NOT_SUPPORTED
* [ ] CLAIM_NOT_FOUND
* [ ] EVIDENCE_INSUFFICIENT
* [ ] QUERY_OUT_OF_SCOPE
* [ ] MODEL_UNAVAILABLE
* [ ] RETRIEVAL_UNAVAILABLE
* [ ] QUERY_TIMEOUT
* [ ] CHAT_STREAM_IN_FLIGHT

同时：

* [ ] Query overall deadline
* [ ] Stage timeout
* [ ] Verify/Repair hard cap
* [ ] timeout 后停止后续模型调用
* [ ] degraded response

---

# P0-FINAL：Patent Controlled Pilot Gate

这批是第一道真正大关。

真实至少跑：

```text
Exact Metadata
Exact Claim RAW
Claim Dependency
Claim Summary
Structured Count
Structured Sum
Structured Avg
Structured Group
Structured TopN
Scoped RAG
Hybrid RAG
Multi-turn
Clarify
Abstain
Permission revoke
Browser refresh
Client cancel
20-turn continuous chat
```

全部确认：

* [ ] route 正确
* [ ] answer 正确
* [ ] Scope 正确
* [ ] Evidence 正确
* [ ] Citation 正确
* [ ] Trace 正确
* [ ] History 正确
* [ ] Permission 正确
* [ ] SSE 正确
* [ ] 无 duplicate message
* [ ] 无无限 loading
* [ ] 无无限 repair
* [ ] 无跨文档污染
* [ ] 无跨 KB
* [ ] 无跨租户

**达到这里：**

> `Patent AI Knowledge Base — Controlled Pilot`

---

# Phase 1：质量工程

P0 过了以后马上做这个，**不要先扩第二行业。**

## P1-01 扩充真实 Patent 数据

目标：

```text
第一阶段：50~100 documents
之后：500+
```

覆盖：

* [ ] 不同申请人
* [ ] 不同 IPC
* [ ] 长文档
* [ ] 短文档
* [ ] 图片
* [ ] 表格
* [ ] 复杂 Claim dependency
* [ ] 多版本
* [ ] 相似专利

---

## P1-02 建立 Eval Dataset

至少：

```text
200~500 questions
```

分类：

* [ ] Metadata
* [ ] Exact Claim
* [ ] Dependency
* [ ] Summary
* [ ] Structured
* [ ] Scoped
* [ ] Hybrid
* [ ] Comparison
* [ ] Multi-turn
* [ ] Ambiguous
* [ ] Missing Knowledge
* [ ] Conflict
* [ ] Out-of-scope
* [ ] Permission

---

## P1-03 建立质量 Gate

建议指标：

```text
Metadata Accuracy        100%
Exact Claim              >=99%

Recall@5                 >=95%

Citation Accuracy        >=98%
Citation Coverage        >=98%

Unsupported Claim Rate   <2%

Abstain Accuracy         >=98%

Wrong Document Rate      <1%
```

再加：

* [ ] Route Accuracy
* [ ] Scope Resolution Accuracy
* [ ] Structured Query Accuracy
* [ ] Multi-turn entity retention
* [ ] Clarification Accuracy

---

## P1-04 Bad Case Regression

每次发现 Bug：

```text
Case
↓
加入 Eval
↓
永远回归
```

禁止修完 Case 后丢掉。

建立：

* [ ] regression dataset
* [ ] route regression
* [ ] retrieval regression
* [ ] citation regression
* [ ] structured regression
* [ ] prompt regression

---

# Phase 2：Feedback → Knowledge Learning

## P2-01 Feedback Analytics

建设：

```text
有用率
差评率
按 KB
按 Domain
按 Route
按模型
按 Prompt
按时间
```

* [ ] Feedback Dashboard
* [ ] Trend
* [ ] Route bad rate
* [ ] Knowledge gap
* [ ] Model regression
* [ ] Prompt regression

---

## P2-02 Bad Case Mining

自动分类：

```text
Wrong Route
Retrieval Miss
Wrong Scope
Knowledge Missing
Knowledge Outdated
Citation Error
Hallucination
Timeout
Model Failure
```

形成：

> AI Quality Issue Pool

---

## P2-03 高频问题聚类

例如：

```text
怎么退款？
退款流程？
我要退费。
```

聚合成同一个 Cluster。

指标：

* [ ] queryCount
* [ ] positiveRate
* [ ] stableEvidence
* [ ] stableAnswer
* [ ] knowledgeVersion

---

## P2-04 FAQ Candidate

高频 + 稳定 + 高好评：

```text
→ FAQ Candidate
```

必须：

```text
→ 人工审核
→ 发布
```

禁止自动入正式知识。

---

## P2-05 FAQ Fast Path

Planner：

```text
RULE
STRUCTURED_QUERY
APPROVED_FAQ
EXACT
SCOPED_RAG
HYBRID_RAG
```

目标：

高频问题：

```text
不再每次：
Embedding → Rerank → LLM → Verify
```

而是：

```text
FAQ Match
→ Validity Check
→ Answer
```

---

# Phase 3：Production Hardening

这里开始决定能不能真正部署给外部客户。

## P3-01 Timeout / Retry / Circuit Breaker

每个服务：

```text
Knowledge
Ingestion
Retrieval
Evidence
Model
Chat
Eval
```

完成：

* [ ] timeout
* [ ] retry policy
* [ ] exponential backoff
* [ ] circuit breaker
* [ ] bulkhead
* [ ] rate limit
* [ ] fallback

---

## P3-02 Graceful Degradation

例如：

```text
Rerank 挂
→ BM25 + Vector

Vector 挂
→ BM25

LLM 挂
→ Exact/Structured 仍可用
```

* [ ] degraded=true
* [ ] degradedReason
* [ ] 用户可理解提示
* [ ] 不暴露内部异常

---

## P3-03 Idempotency

重点：

* [ ] upload
* [ ] ingest
* [ ] publish
* [ ] retry
* [ ] stream send
* [ ] feedback
* [ ] version generation
* [ ] index write
* [ ] job retry

避免：

```text
重复 Chunk
重复 Message
重复 Index
重复 Version
```

---

## P3-04 Durable Task / DLQ

现在所有关键异步任务最终要：

* [ ] durable task
* [ ] retry counter
* [ ] max retry
* [ ] next retry
* [ ] dead-letter
* [ ] manual retry
* [ ] audit history

不要继续依赖裸：

```text
new Thread(...)
```

---

## P3-05 Index Reconcile

非常重要。

建立：

```text
MySQL Source of Truth
     ↓
ES
     ↓
Milvus
```

定期检查：

* [ ] missing
* [ ] stale
* [ ] duplicate
* [ ] deleted-but-present
* [ ] wrong version
* [ ] wrong tenant
* [ ] wrong KB

支持：

```text
scan
repair
report
```

---

# Phase 4：Enterprise Security

外部客户试用前，这是硬 Gate。

## P4-01 ACL 全链路

权限必须贯穿：

```text
Tenant
Workspace
KB
Document
Version
Chunk
Evidence
Conversation
Structured Query
```

* [ ] Exact ACL
* [ ] BM25 ACL
* [ ] Vector ACL
* [ ] Structured ACL
* [ ] Evidence ACL
* [ ] History ACL
* [ ] Trace ACL
* [ ] Export ACL

特别检查此前出现过的：

```text
checkKnowledgePermission(...)
```

如果仍有 `return true` 类型实现：

> 必须清零。

---

## P4-02 企业认证

后续：

* [ ] OIDC
* [ ] OAuth2
* [ ] LDAP
* [ ] 企业微信/钉钉按客户需求
* [ ] SAML（大型客户阶段）

---

## P4-03 Security Governance

* [ ] secrets management
* [ ] API key 加密
* [ ] TLS
* [ ] DB encryption / sensitive field encryption
* [ ] PII masking
* [ ] prompt/data leakage controls
* [ ] upload malware/extension validation
* [ ] audit
* [ ] retention
* [ ] deletion
* [ ] export control

---

# Phase 5：Knowledge Ops 完整化

企业知识库不能只是“上传 → RAG”。

## P5-01 Knowledge Lifecycle

完善：

```text
DRAFT
PARSED
REVIEW
PUBLISHED
SUPERSEDED
EXPIRED
ARCHIVED
FAILED
```

* [ ] 审核
* [ ] 发布
* [ ] 撤回
* [ ] 失效
* [ ] 回滚
* [ ] 新版本
* [ ] 历史版本

---

## P5-02 Conflict Engine

检测：

```text
同实体
同条件
不同答案
```

支持：

* [ ] conflict detection
* [ ] authority ranking
* [ ] effective time
* [ ] manual arbitration
* [ ] conflict history

---

## P5-03 Temporal Knowledge

企业知识经常是：

```text
2025 年有效
2026 年失效
```

支持：

* [ ] effectiveFrom
* [ ] effectiveTo
* [ ] publishedAt
* [ ] supersededBy
* [ ] temporal query
* [ ] current-valid filtering

---

# Phase 6：Observability / AI Ops

## P6-01 Metrics

至少：

```text
QPS
P50/P95/P99
Error Rate
Timeout Rate

Route Distribution

Embedding latency
BM25 latency
Vector latency
Rerank latency
LLM latency

Token
Cost
Feedback rate
Useful rate
```

---

## P6-02 Alert

至少：

* [ ] Model unavailable
* [ ] Vector unavailable
* [ ] ES unavailable
* [ ] Redis unavailable
* [ ] DB pool
* [ ] Queue backlog
* [ ] ingestion fail spike
* [ ] citation accuracy drop
* [ ] timeout spike
* [ ] cost spike

---

## P6-03 SLO

正式定义，例如：

```text
Structured / Exact
P95 < 2s

Claim Summary
P95 < 10s

Scoped
P95 < 15s

Hybrid
P95 < 20s
```

同时：

```text
Availability
Error budget
```

---

# Phase 7：Scale Test

然后才开始真正验证千万级。

数据量：

```text
10万
↓
100万
↓
1000万知识单元
```

测试：

* [ ] ingestion throughput
* [ ] ES index size
* [ ] Milvus size
* [ ] BM25 P95
* [ ] ANN P95
* [ ] scalar filtering
* [ ] ACL filter
* [ ] tenant isolation
* [ ] concurrent users
* [ ] first token latency
* [ ] complete answer latency
* [ ] cost

架构要求：

```text
Tenant
KB
Domain
Region
Version
Effective Time
ACL
Document

先 Filter
↓
再 ANN
```

禁止：

```text
全库 ANN TopK
↓
Java 事后过滤
```

---

# Phase 8：第二 Domain 验证

Patent 跑稳后，第二个我还是建议：

> **Telecom**

因为它可以强测我们的通用架构。

要验证：

```text
省
地市
套餐
价格
渠道
客户类型
生效时间
失效时间
互斥条件
地域差异
```

目标：

```text
新增 Telecom Domain Pack
```

而不是：

```text
修改 Core 30 个 if/else
```

验收：

* [ ] Core 基本不改
* [ ] 注册 Entity
* [ ] 注册 Metric
* [ ] 注册 Dimension
* [ ] 注册 aliases
* [ ] 注册 Structured Adapter
* [ ] Retrieval Policy
* [ ] Authority Policy
* [ ] Temporal Policy
* [ ] Eval Dataset

这一步如果成立，才证明：

> **Domain Pack 架构是真的通用，不是 Patent 项目换皮。**

---

# Phase 9：企业级交付能力

最后补客户真正会问的东西。

* [ ] Docker / K8s 部署
* [ ] Helm
* [ ] environment config
* [ ] rolling upgrade
* [ ] migration rollback
* [ ] backup
* [ ] restore
* [ ] disaster recovery
* [ ] MySQL backup
* [ ] MinIO backup
* [ ] ES restore
* [ ] Milvus restore
* [ ] health check
* [ ] capacity planning
* [ ] deployment guide
* [ ] operation guide
* [ ] troubleshooting guide
* [ ] SLA
* [ ] security checklist
* [ ] release checklist

---

# 最终五个产品 Gate

以后不要再用“开发了多少功能”衡量进度，直接用这五个 Gate：

| Gate   | 达到什么状态                                 | 可以叫什么               |
| ------ | -------------------------------------- | ------------------- |
| **G1** | P0 主链全部 PASS                           | Functional Beta     |
| **G2** | 真实 Eval 达标                             | Controlled Pilot    |
| **G3** | Security + Reliability + Observability | Production Ready    |
| **G4** | 100万/1000万级 + 第二 Domain 验证             | Enterprise Platform |
| **G5** | SSO/灾备/SLA/K8s/企业交付体系                  | Enterprise Ready    |

### 当前最短执行路径

```text
现在
 ↓
SSE Final Verify
 ↓
通用 Structured Query
 ↓
Evidence / Citation / History
 ↓
Trace
 ↓
Workbench
 ↓
Feedback
 ↓
Error / Deadline
 ↓
P0 Final
========================
 ↓
50~100 Patent
 ↓
200~500 Eval Questions
 ↓
Quality Gate
========================
 ↓
Security
Reliability
Reconcile
Observability
========================
 ↓
Feedback Learning / FAQ
========================
 ↓
10万 → 100万 → 1000万
========================
 ↓
Telecom Domain Pack
========================
 ↓
Enterprise Delivery
```

**现在最关键的是把第一条分界线之前全部做完。** 到那里先停开发，正式跑一次 P0 Final；通过后再进入 Eval，不要中途继续扩业务功能。
