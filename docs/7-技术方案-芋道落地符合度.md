# 技术方案符合度对照:芋道 yudao-cloud-mini(jdk17)落地核查

> 版本:v1.0 · 2026-08-15
> 依据:《4-技术方案.md》第 4 节技术选型 / 第 5 节数据层,对照本机芋道仓库实际依赖(已逐项核实 pom 与 starter)。
> 结论先行:**核心中间件已满足,6 项需补充依赖,其余为已确认的差异项。**

---

## 1. 已满足(芋道内置或可启用)

| 技术方案选型 | 芋道实际 | 状态 |
|-------------|---------|------|
| Spring Boot 3.x | Spring Boot 3.5.15 | ✅ |
| Spring Cloud / Spring Cloud Alibaba | 2025.0.1 / 2025.0.0.0 | ✅ |
| API 网关(Spring Cloud Gateway) | yudao-gateway(48080) | ✅(方案另提的 APISIX 为生产可选项) |
| ORM: MyBatis-Plus | 3.5.16 + mybatis-plus-join | ✅ |
| 数据库: PostgreSQL(已决策改 MySQL) | MySQL 8(库 ruoyi-vue-pro) | ✅(决策项) |
| 缓存: Redis | Redisson 4.6.1(starter-redis) | ✅ |
| 消息: Kafka | starter-mq 内置 spring-kafka(另含 RabbitMQ/RocketMQ) | ✅ |
| 对象存储: MinIO / S3 / OSS | infra 文件服务(S3 协议,可配 MinIO) | ✅ |
| 配置中心 / 注册中心: Nacos | nacos-discovery + nacos-config(namespace=dev) | ✅ |
| 安全: Spring Security + JWT | starter-security(Token + Redis,支持 SSO) | ✅ |
| 定时任务 | xxl-job 2.4.0(starter-job) | ✅ |
| 工作流: Flowable | Flowable 8.0.0(bpm 模块注释中,按需启用) | ✅ |
| API 文档: OpenAPI / Swagger | springdoc 2.8.17 + knife4j 4.5.0 | ✅ |
| 链路: OpenTelemetry / Jaeger | OpenTelemetry 1.39.0 + SkyWalking 9.6.0 | ✅ |
| 实时通信(对话流式) | starter-websocket | ✅(SSE 亦可) |
| 文件类型识别: Apache Tika | tika-core 3.3.1(仅类型识别,解析需补) | ⚠️ 部分 |
| 消息队列(备选) | RocketMQ 2.3.6 内置 | ✅ |

## 2. 需补充依赖(技术方案要求,芋道未内置)

| 选型 | 用途 | 补充方式 |
|------|------|---------|
| Elasticsearch | 检索平台:BM25 全文 + 元数据过滤 | 加 `spring-data-elasticsearch` 依赖,连 localhost:9200 |
| Milvus SDK | 向量召回(技术方案 5.3 决策) | 加 `milvus-sdk-java`(或 spring-ai 的 vectorstore-milvus),连 19530 |
| Drools | 规则引擎(PRD BR-011) | 加 `drools-core` / `drools-compiler` 依赖,rule-server 使用 |
| Tika Parsers + PDFBox + POI | 文档解析(入库管线,技术方案 5.2) | knowledge/ingestion 模块补 `tika-parsers`、`pdfbox`、`poi` |
| Spring AI(可选) | 芋道自带 spring.ai 配置但未引入依赖 | 若复用芋道 AI 能力再加;本项目 model-gateway 自研,非必需 |
| Prometheus 指标暴露 | 监控(方案 7 非功能 N4) | 芋道已有 actuator 端点,接 Prometheus 采集即可,无需代码 |

## 3. 差异项(已确认 / 可选项)

| 项 | 技术方案 | 芋道落地 | 说明 |
|----|---------|---------|------|
| JDK | 21 | **17**(master-jdk17 分支) | 已接受;团队按 17 开发 |
| 数据库 | PostgreSQL + pgvector | MySQL 8 | 已决策(向量已迁 Milvus,PG 无优势) |
| 网关 | APISIX / SCG | Spring Cloud Gateway | 开发足够;生产如需 APISIX 可前置 |
| 日志采集 | Loki / ELK | logback 文件日志 | 生产期再接 ELK,不阻塞开发 |
| 监控看板 | Grafana | Spring Boot Admin + actuator | 生产期接 Prometheus/Grafana |
| 密钥管理 | KMS / Vault | 未内置 | 生产安全加固项,后补 |

## 4. 按模块的依赖就绪度

| AI 模块 | 依赖就绪度 | 待补 |
|--------|-----------|------|
| chat | ✅ 可启动 | SSE 链路依赖 retrieval/evidence/model(Feign 已备) |
| knowledge | ⚠️ | 文档解析需补 tika-parsers/pdfbox/poi;文件上传走 infra FileApi(已就绪) |
| ingestion | ⚠️ | Kafka 已就绪(starter-mq);ES/Milvus 写入需补依赖 |
| retrieval | ⚠️ | ES + Milvus 依赖待补 |
| evidence | ✅ 可启动 | 无额外中间件 |
| model | ✅ 可启动 | 模型 API 客户端自研(或用 spring-ai 可选) |
| rule | ⚠️ | Drools 依赖待补 |
| agent / workflow | ✅ | workflow 可切芋道 bpm(Flowable 已就绪) |
| eval | ✅ | 无额外中间件 |
| governance | ✅ | 无额外中间件 |

## 5. 建议动作

1. **先跑通**:按现有依赖启动 knowledge/evidence/chat,验证模块链路(不影响 ES/Milvus)
2. **补齐依赖**:在开发 retrieval/ingestion 前,给这几个模块的 pom 补 ES、Milvus、Drools、Tika Parsers/PDFBox/POI(可生成统一 pom 补丁)
3. **中间件**:docker-compose 里 ES(9200)、Kafka(9092)、Milvus(19530)已备好,按需 `docker compose up`
